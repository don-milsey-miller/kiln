[
  {
    "environment": {
      "execution": "host",
      "facts": {
        "chokidar": "5.0.0",
        "node": "v24.18.0",
        "os": "Windows 11"
      }
    },
    "id": "EVD-0001",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": " ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-17",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "400 file creations, three runs of three. fs.watch reported 1 distinct path with no error; chokidar reported 400/400.",
    "title": "Watcher spike, check 2",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "node": "v24.18.0",
        "os": "Windows 11"
      }
    },
    "id": "EVD-0002",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": " ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-18",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Six node processes creating requirements concurrently received six distinct IDs. Falsified by removing the lock: eight unlocked processes produced 4 distinct IDs of 8.",
    "title": "Six concurrent processes, distinct IDs",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "node": "v24.18.0",
        "os": "Windows 11"
      }
    },
    "id": "EVD-0003",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": " ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-18",
    "outcome": "failure",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "The same test failed once during a full 92-test parallel run and did not reproduce in three subsequent runs. The failure output was not captured, so the cause is unconfirmed; maxWaitMs was raised as a mitigation on an unproven diagnosis.",
    "title": "One unexplained failure in a full parallel run",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "lockAcquisitionRetry": "EEXIST only",
        "node": "v24.18.0",
        "os": "Windows 11"
      }
    },
    "id": "EVD-0004",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "Instrumentation validated before use: forcing maxWaitMs=1 produced five children classified lock-acquisition-timeout and the assertion 'NOT a serialisation result', confirming the classifier distinguishes causes rather than reporting the first assertion to trip. ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-18",
    "outcome": "failure",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Instrumented the concurrency test so each child reports a structured outcome, then ran the isolated test file repeatedly. Run 19 of a declared 40 failed. Classification: one child of six, `lock-acquisition-timeout` class, message `Could not acquire ....planning.lock: EPERM`, elapsed 507ms. No duplicate IDs; the other five children allocated distinct IDs. openSync(path,'wx') returned EPERM rather than EEXIST, and the retry loop treated anything but EEXIST as fatal.",
    "title": "Recurrence reproduced and classified — lock acquisition returned EPERM",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "lockAcquisitionRetry": "EEXIST/EPERM/EBUSY/EACCES",
        "node": "v24.18.0",
        "os": "Windows 11"
      }
    },
    "id": "EVD-0005",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": " ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-18",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "After widening acquisition retry to EEXIST/EPERM/EBUSY/EACCES: isolated concurrency file 40/40 passed; full suite at normal parallelism 30/30 passed; full suite serial 10/10 passed. Counts declared before running. Bounded non-reproduction, not proof of absence: at a 10% per-run rate, 30 parallel runs would miss a recurrence about 4% of the time.",
    "title": "Post-fix matrix: 80 runs, no recurrence",
    "type": "evidence"
  },
  {
    "id": "EVD-0006",
    "kind": "source",
    "lifecycle": "active",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/getting-started/caching",
        "retrievedAt": "2026-08-18",
        "title": "Next.js — Getting Started: Caching"
      }
    ],
    "summary": "States that module imports, synchronous I/O and pure computations are 'predictable values': 'Components using only these operations are prerendered automatically, and their output becomes part of the static HTML at build time.' Lists `fs.readFileSync` explicitly among them. Contrasts this with `await readFile()` inside a component, which 'would be treated as uncached data that must be either accessed within `use cache` or behind a `<Suspense>` boundary.' Applies when cacheComponents: true.",
    "title": "Next.js docs — Caching (Cache Components model)",
    "type": "evidence"
  },
  {
    "id": "EVD-0007",
    "kind": "source",
    "lifecycle": "active",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/guides/caching-without-cache-components",
        "retrievedAt": "2026-08-18",
        "title": "Next.js — Caching and Revalidating (Previous Model)"
      }
    ],
    "summary": "Applies when cacheComponents is NOT enabled, which is the default. States under route segment `revalidate`: 'In Development, Pages are always rendered on-demand and are never cached. This allows you to see changes immediately without waiting for a revalidation period to pass.'",
    "title": "Next.js docs — Caching and Revalidating (Previous Model)",
    "type": "evidence"
  },
  {
    "id": "EVD-0008",
    "kind": "source",
    "lifecycle": "active",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/api-reference/functions/revalidatePath",
        "retrievedAt": "2026-08-18",
        "title": "Next.js — revalidatePath"
      },
      {
        "locator": "https://github.com/vercel/next.js/discussions/81385",
        "retrievedAt": "2026-08-18",
        "title": "vercel/next.js discussion 81385 — understanding revalidatePath"
      }
    ],
    "summary": "revalidatePath/revalidateTag run only in Server Actions or Route Handlers and cannot be called from a Client Component. Invalidation plus the client Router Cache purge is triggered BY the server action the user performed; nothing observes writes made by a process outside the app.",
    "title": "Next.js docs and issue tracker — revalidatePath is server-triggered only",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "cacheComponents": "both",
        "framework": "Next.js 16.3.1",
        "mode": "next dev",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "readApi": "both"
      }
    },
    "id": "EVD-0009",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "Kept as the summary of the whole run and deliberately UNATTACHED. Its environment says cacheComponents:'both' and readApi:'both', which environment-MISMATCHES every claim about one configuration — the applicability filter excluded it from all four, correctly, and that is how the modelling error was caught. A matrix run is N experiments and must be recorded as N. The per-cell records carry the bearing; this one carries the protocol. ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-18",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Minimal Next.js 16.3.1 app reading data/artifact.json in a Server Component. Matrix: read API {fs.readFileSync, await readFile} x cacheComponents {disabled, enabled}. Protocol per cell: render A; write B from a separate process touching no application code; wait 10s; read module-scope server counters via a route handler so browser behaviour cannot be mistaken for server caching; then make a fresh request. RESULT — ALL FOUR CELLS IDENTICAL: nothing happened during the quiet window (0 page requests, 0 file reads); the fresh request returned B; counters confirmed the server re-read the file and re-rendered. Step 6: an app-supplied SSE route watching the data directory delivered a change event to a connected client within the window in both cacheComponents modes, and a reload then showed B.",
    "title": "Next.js probe — matrix SUMMARY (not attachable to a single-cell claim)",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "cacheComponents": "disabled",
        "framework": "Next.js 16.3.1",
        "mode": "next dev",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "readApi": "fs.readFileSync"
      }
    },
    "id": "EVD-0010",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": " ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-18",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Rendered A; wrote B from a separate process touching no application code; waited 10s. Nothing happened during the quiet window (0 page requests, 0 file reads). A fresh request returned B, and module-scope server counters confirmed the server re-read the file and re-rendered — so the default model serves fresh data in dev.",
    "title": "Probe cell — fs.readFileSync, cacheComponents disabled",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "cacheComponents": "disabled",
        "framework": "Next.js 16.3.1",
        "mode": "next dev",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "readApi": "await readFile"
      }
    },
    "id": "EVD-0011",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": " ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-18",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Rendered A; wrote B from a separate process touching no application code; waited 10s. Nothing happened during the quiet window (0 page requests, 0 file reads). A fresh request returned B, and module-scope server counters confirmed the server re-read the file and re-rendered — so an awaited read is equally fresh in dev.",
    "title": "Probe cell — await readFile, cacheComponents disabled",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "cacheComponents": "enabled",
        "framework": "Next.js 16.3.1",
        "mode": "next dev",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "readApi": "fs.readFileSync"
      }
    },
    "id": "EVD-0012",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": " ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-18",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Rendered A; wrote B from a separate process touching no application code; waited 10s. Nothing happened during the quiet window (0 page requests, 0 file reads). A fresh request returned B, and module-scope server counters confirmed the server re-read the file and re-rendered — so a SYNCHRONOUS read did NOT freeze into a static shell in dev.",
    "title": "Probe cell — fs.readFileSync, cacheComponents enabled",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "cacheComponents": "enabled",
        "framework": "Next.js 16.3.1",
        "mode": "next dev",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "readApi": "await readFile"
      }
    },
    "id": "EVD-0013",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": " ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-18",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Rendered A; wrote B from a separate process touching no application code; waited 10s. Nothing happened during the quiet window (0 page requests, 0 file reads). A fresh request returned B, and module-scope server counters confirmed the server re-read the file and re-rendered — so an awaited read behaves as uncached data in dev.",
    "title": "Probe cell — await readFile, cacheComponents enabled",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "cacheComponents": "disabled",
        "framework": "Next.js 16.3.1",
        "mode": "next dev",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "writePath": "route handler -> writeReviewStatus"
      }
    },
    "id": "EVD-0014",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": " ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-18",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "A Next.js route handler called the repo's REAL writeReviewStatus (lock -> fresh read inside the lock -> validate -> atomic write) via runtime dynamic import; nothing was reimplemented. Results: the write landed and reviewStatus changed; every other field byte-identical, so the reviewStatus-only boundary held; lock released; no temp file left. A request asking for reviewStatus 'retired' was refused 400 and the artifact was unchanged. With the lock held by an EXTERNAL process, the Next.js write did not proceed (artifact unchanged after 1.2s), then completed once the lock was released — total wait ~1.3s. React's state model did not interfere with the lock in either mode.",
    "title": "Write-back cell — cacheComponents disabled",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "cacheComponents": "enabled",
        "framework": "Next.js 16.3.1",
        "mode": "next dev",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "writePath": "route handler -> writeReviewStatus"
      }
    },
    "id": "EVD-0015",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": " ⚠️ MIGRATED 2026-08-22 (#131): this record previously carried `environment.tier: 1`. It was not an observation — the old schema REQUIRED a sandbox tier, and tier 1 was the only value that fit a run with no controller. No validation controller existed when this was recorded, so the run executed directly on the host and is now recorded as `execution: \"host\"` with no tier. The value is dropped rather than kept because keeping it would claim isolation the run never had.",
    "observedAt": "2026-08-18",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "A Next.js route handler called the repo's REAL writeReviewStatus (lock -> fresh read inside the lock -> validate -> atomic write) via runtime dynamic import; nothing was reimplemented. Results: the write landed and reviewStatus changed; every other field byte-identical, so the reviewStatus-only boundary held; lock released; no temp file left. A request asking for reviewStatus 'retired' was refused 400 and the artifact was unchanged. With the lock held by an EXTERNAL process, the Next.js write did not proceed (artifact unchanged after 1.2s), then completed once the lock was released — total wait ~1.3s. React's state model did not interfere with the lock in either mode.",
    "title": "Write-back cell — cacheComponents enabled",
    "type": "evidence"
  },
  {
    "id": "EVD-0016",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"1,000 credits/month\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://docs.tavily.com/documentation/api-credits",
        "retrievedAt": "2026-08-22",
        "title": "Credits & Pricing - Tavily Docs"
      }
    ],
    "summary": "Tavily's published documentation states the free plan provides 1,000 credits per month",
    "title": "Source: Credits & Pricing - Tavily Docs",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "backend": "tavily",
        "credential-source": "host environment variable",
        "os": "Windows",
        "quota-limit": "1500",
        "quota-used": "0"
      },
      "omissions": [
        {
          "fact": "node-version",
          "reason": "No capture plan existed: this run predates the validation controller. It was not collected then, and it must not be filled in now from a re-run.",
          "state": "not-captured"
        },
        {
          "fact": "os-build",
          "reason": "Nothing collected it at the time.",
          "state": "not-captured"
        },
        {
          "fact": "tavily-plan-name",
          "reason": "The /usage response body was not retained; only the figures the probe printed survive.",
          "state": "not-captured"
        },
        {
          "fact": "timing",
          "reason": "The probe does not time itself, and no external timing was recorded.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0017",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ PROVENANCE: executed by the PM in their own terminal and the OUTPUT WAS PASTED. Not collected by an instrumented run, so what survives is the eight lines it printed and nothing more. ⚠️ RECORDED 2026-08-22 FROM THAT ORIGINAL OUTPUT, four days of decisions later, and deliberately NOT re-run. A fresh probe would observe a fresh moment — by now at least one search credit has been spent — and presenting it as evidence of the 1500/1500, 0-used event would be a later observation wearing an earlier date. ⚠️ `execution: host` with NO sandboxTier: this ran directly on the machine, so it makes no isolation claim (#131). The facts above are exactly those the output carried; everything else is listed as an omission with its reason.",
    "observedAt": "2026-08-22",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "`npm run research:probe` printed: available, backend=tavily, probed live, no search credit spent; quota 1500 of 1500 credits remaining (0 used); tools research_capability, research_search, research_fetch.",
    "title": "npm run research:probe, PM's host, 2026-08-22",
    "type": "evidence"
  },
  {
    "id": "EVD-0018",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"Development builds output to `.next/dev` instead of `.next`. This allows you to run `next dev` and `next build` concurrently without conflicts.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/api-reference/cli/next",
        "retrievedAt": "2026-08-24",
        "title": "https://nextjs.org/docs/app/api-reference/cli/next"
      }
    ],
    "summary": "Next.js 16.3.2 development builds output to .next/dev instead of .next, so dev and production are different build artifacts",
    "title": "Source: https://nextjs.org/docs/app/api-reference/cli/next",
    "type": "evidence"
  },
  {
    "id": "EVD-0019",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"`next start` starts the application in production mode. The application should be compiled with\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/api-reference/cli/next",
        "retrievedAt": "2026-08-24",
        "title": "https://nextjs.org/docs/app/api-reference/cli/next"
      }
    ],
    "summary": "next start runs production mode and requires a prior next build",
    "title": "Source: https://nextjs.org/docs/app/api-reference/cli/next",
    "type": "evidence"
  },
  {
    "id": "EVD-0020",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"Node.js deployments support all Next.js features.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/getting-started/deploying",
        "retrievedAt": "2026-08-24",
        "title": "https://nextjs.org/docs/app/getting-started/deploying"
      }
    ],
    "summary": "Node.js self-hosted deployment is supported and supports all Next.js features",
    "title": "Source: https://nextjs.org/docs/app/getting-started/deploying",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "install": "local to the temporary directory; no global install, no repository change",
        "network": "loopback only for the dev server; npm install reached the public registry",
        "nextVersion": "16.3.2",
        "node-version": "v24.18.0",
        "os": "win32 x64",
        "workspace": "dedicated temporary directory, removal verified"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation",
          "freedom-from-concurrent-host-activity"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "sustained-runtime-behaviour",
          "reason": "The dev server ran for well under a minute. Handle count, memory growth and watcher stability over hours were not measured, and that is the actual concern behind QST-0017 rather than the burst behaviour this did measure.",
          "state": "not-captured"
        },
        {
          "fact": "content-root-outside-app-root",
          "reason": "The watched directory sat inside the Next.js project root. An application watching a content root OUTSIDE the app root is a different arrangement and was not exercised.",
          "state": "not-captured"
        },
        {
          "fact": "other-operating-systems",
          "reason": "One host was available. #73 already records that file-watching behaviour differs by platform, so this result is scoped to Windows and cannot be generalised from here.",
          "state": "not-observable"
        },
        {
          "fact": "concurrent-host-activity",
          "reason": "Execution was on the host with no containment, so whatever else the machine was doing during the runs is unknown. The two controls bound this rather than remove it: an unrelated disturbance would have had to affect the treatment alone.",
          "state": "unavailable"
        }
      ]
    },
    "id": "EVD-0021",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ HOST EXECUTION, stated rather than implied. The tier-1 controller could not run this at all: the job declares `requires: { network: true }` for `npm install` and the approved ceiling refuses it before provisioning (#77), and — separately and more fundamentally — the job model has no form for a long-lived service alongside commands, so the probe is not expressible under it regardless of the ceiling. See the capability gap recorded as its own question. ⚠️ Controls before AND after the treatment, so 'no interference' is a comparison rather than an assertion: a second control isolates interference from any second-run effect. ⚠️ The workspace was removed and its ABSENCE checked rather than the removal call trusted — the same discipline the controller uses, applied by hand because no controller ran. ⚠️ METHODOLOGY HISTORY — AN INVALIDATED FIRST RUN, EXPLICITLY EXCLUDED FROM THE FINDING ABOVE. An earlier execution of this probe reported treatment 0/20 with the controls at 20/20, which read as total interference from `next dev`. It was a FIXTURE DEFECT, not an observation: both runs wrote the same filenames into the same directory, so the control created the files and the treatment only modified them, while the watcher listened for `add`. A second defect in the same version measured latency as elapsed-time-since-watcher-start, which reports the write schedule (20 files x 120ms) rather than detection lag and would have read as ~2.4s of latency on a completely idle watcher. ⚠️ NONE OF THAT RUN'S NUMBERS CONTRIBUTE ANYTHING HERE. They are void rather than weak, and the summary, outcome and omissions above describe only the corrected execution: a fresh directory per run, per-file observed-minus-written latency, and a control taken after the treatment as well as before. Both defects were in the fixture; neither touched the code under observation. ⚠️ It is preserved because the correction is WHY this result can be trusted rather than a detail about how it was obtained. The control is what exposed it — a result that dramatic, from a change that small, is a bug before it is a finding — and a probe whose only reported run is the one that worked has not demonstrated that it could have failed.",
    "observedAt": "2026-08-25",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Three runs of the same measurement in one disposable workspace. control: 20/20 observed, median lag 3ms, max 6ms; treatment: 20/20 observed, median lag 3ms, max 10ms with `next dev` serving on 127.0.0.1:3987; control2: 20/20 observed, median lag 3ms, max 7ms after the dev server was killed. No file was missed in any run, and `next dev` did not recompile during the content writes.",
    "title": "Experiment: chokidar detection with and without a concurrent `next dev`",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "network": "loopback only for `next start`; npm install reached the public registry",
        "node": "v24.18.0",
        "node-version": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "readApi": "fs.readFileSync",
        "workspace": "two dedicated temporary directories, removal verified"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "revalidation-beyond-ten-seconds",
          "reason": "Requests were made immediately after the external write and again six seconds later. A revalidation window longer than that would not have been seen, so this shows the value is frozen across ten seconds, not that it is frozen forever.",
          "state": "not-captured"
        },
        {
          "fact": "dynamic-route-configurations",
          "reason": "Only the default static outcome was exercised. Whether an explicitly dynamic route, or a read through a dynamic API, reflects external writes was not tested — and that is precisely what the application needs to know next.",
          "state": "not-captured"
        },
        {
          "fact": "other-operating-systems",
          "reason": "One host was available; this is scoped to Windows and cannot be generalised from here.",
          "state": "not-observable"
        }
      ]
    },
    "id": "EVD-0022",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ BOTH CONFIGURATIONS WERE RUN ON PURPOSE. The claim names the cacheComponents-enabled case, and testing only that case would have left the undecided configuration as a third deferral — which is the outcome #80's condition exists to prevent. Running both is what produced the second finding. ⚠️ Host execution: the tier-1 controller could not run this (network for `npm install`, and no long-lived service form — QST-0022). Two disposable workspaces, removal observed rather than trusted, whole process tree terminated between configurations.",
    "observedAt": "2026-08-25",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Two full builds. With cacheComponents ENABLED the route built as \"┌ ○ /\" and served \"VALUE_AT_BUILD_TIME\" on the first request; after an external write of a different value it still served \"VALUE_AT_BUILD_TIME\", and \"VALUE_AT_BUILD_TIME\" six seconds later. With cacheComponents DISABLED the result was identical: route \"┌ ○ /\", and the same build-time value on all three requests. In both configurations a later external write did not change what a fresh request returned.",
    "title": "Experiment: production-build artifact read, Cache Components enabled and disabled",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "network": "loopback only for `next start`; npm install reached the public registry",
        "node": "v24.18.0",
        "node-version": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "readApi": "fs.readFileSync",
        "workspace": "one disposable workspace per pass, removal verified"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "revalidation-beyond-five-seconds",
          "reason": "Each variant was re-requested about two and five seconds after the external write. A longer revalidation window would not have been seen.",
          "state": "not-captured"
        },
        {
          "fact": "concurrent-request-load",
          "reason": "One request at a time. Whether a dynamic route holds up under concurrent requests reading the same artifacts was not exercised, and bears on the runtime lifecycle question rather than on freshness.",
          "state": "not-captured"
        },
        {
          "fact": "other-operating-systems",
          "reason": "One host was available; scoped to Windows.",
          "state": "not-observable"
        }
      ]
    },
    "id": "EVD-0023",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ TWO INSTRUMENT DEFECTS were found and corrected before these numbers were trusted, and both would have read as findings. (1) The marker extractor sliced between two sentinels in the HTML; under Cache Components the Suspense fallback ships in the static shell and the value STREAMS, so the first match landed inside escaped RSC flight data and both Cache Components runs scored 'inconclusive' while plainly serving the right value. Replaced with sentinel presence, which is immune to escaping because only one sentinel is on disk at a time. (2) The route-symbol matcher did not know `◐` (partially prerendered), so it fell through and reported `○ /_not-found` as the route under test. Both defects were in the instrument, not in Next.js. ⚠️ The baseline was re-run in the same pass as the corrected variants rather than carried over from the earlier pass, so every row here was measured by ONE instrument.",
    "observedAt": "2026-08-25",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Seven distinct mechanism/caching-model combinations built and served under `next build` + `next start`, each measured by writing a different value to the artifact after the server was live and re-requesting. Results: baseline/cacheComponents:false -> ┌ ○ / frozen; baseline/cacheComponents:true -> ┌ ○ / frozen; connection/cacheComponents:false -> ┌ ƒ / FRESH; connection/cacheComponents:true -> BUILD FAILED; force-dynamic/cacheComponents:false -> ┌ ƒ / FRESH; baseline/cacheComponents:false -> ┌ ○ / frozen; baseline/cacheComponents:true -> ┌ ○ / frozen; connection-suspense/cacheComponents:false -> ┌ ƒ / FRESH; connection-suspense/cacheComponents:true -> ┌ ◐ / FRESH; io-suspense/cacheComponents:false -> ┌ ○ / frozen; io-suspense/cacheComponents:true -> ┌ ◐ / FRESH. Only `connection()` behind a Suspense boundary reflected the external write in BOTH caching models.",
    "title": "Experiment: which freshness mechanisms survive a production build",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "network": "loopback only for `next start`; npm install reached the public registry",
        "node": "v24.18.0",
        "node-version": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "readApi": "fs.readFileSync",
        "workspace": "one disposable workspace, installed once, rebuilt per configuration; removal verified"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "prefetch-latency",
          "reason": "The cost DEC-0019 accepts — that `connection()` blocks prefetching — was NOT measured here. It is taken from the Next.js documentation, so the accepted cost sits at source-supported while the benefit sits at environment-matched. Measuring it needs client-side navigation between routes, which this probe does not perform.",
          "state": "not-captured"
        },
        {
          "fact": "revalidation-beyond-five-seconds",
          "reason": "Routes were re-requested about two and five seconds after the external write. This shows the control route is frozen across five seconds, not that it is frozen forever — the same bound EVD-0023 carries.",
          "state": "not-captured"
        },
        {
          "fact": "integration-with-the-project-content-loaders",
          "reason": "The application shell does not exist yet, so reads went through `fs.readFileSync` and `JSON.parse` on copies of real artifacts. That matches the READ SHAPE the requirements need but not the project's own loader modules, which the shell is required to reuse rather than rewrite.",
          "state": "not-observable"
        },
        {
          "fact": "concurrent-request-load",
          "reason": "One request at a time. Whether a dynamic route holds up under concurrent reads of the same artifacts bears on runtime lifecycle, not on freshness.",
          "state": "not-captured"
        },
        {
          "fact": "other-operating-systems",
          "reason": "One host was available; scoped to Windows.",
          "state": "not-observable"
        }
      ]
    },
    "id": "EVD-0024",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THIS IS NOT A RE-RUN OF EVD-0023, and the difference is the point. EVD-0023 compared MECHANISMS, one read per build, to choose one. This measures the CONTRACT AS WRITTEN — `connection()` before EACH read, a fallback that is a real loading state — and it carries a NEGATIVE CONTROL inside the same build. Without the control, a fresh result would not distinguish the contract doing the work from something incidental about the build. The control was stale in both configurations, so the contract is load-bearing rather than decorative. ⚠️ THE CONTROL INDEPENDENTLY REPRODUCES AST-0019 a third time, in a third instrument: `○ /bare` built static and served build-time content with cacheComponents both enabled and disabled. ⚠️ THE MIXED ROUTE IS THE FINDING NOBODY ASKED FOR. Its non-compliant read came back FRESH in both configurations, because a compliant sibling had already made the route dynamic. See AST-0022 — it is the reason the 'each read' clause is not redundant.",
    "observedAt": "2026-08-26",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "One application, three routes, built and served twice under `next build` + `next start`. The contract route makes two planning-content reads, each awaiting `connection()` inside its own Suspense boundary with a named loading fallback. The control route makes the same read with neither. A third route mixes one of each. Copies of real planning artifacts were read through `JSON.parse(readFileSync(...))` and a markdown read, then rewritten externally with the server live. With cacheComponents DISABLED the routes built as \"┌ ƒ /\", \"└ ƒ /mixed\" and \"├ ○ /bare\"; with it ENABLED, \"┌ ◐ /\", \"└ ◐ /mixed\" and \"├ ○ /bare\". In BOTH configurations the contract route served the externally written values on every request after the write, its Suspense fallback was present in the initial HTML, and the control route served its build-time values on every request including five seconds later. The mixed route served BOTH slots fresh — including the non-compliant one.",
    "title": "Experiment: the DEC-0019 read contract, with a non-compliant control on the same build",
    "type": "evidence"
  },
  {
    "id": "EVD-0025",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"Expressions can contain whole JavaScript programs as long as they’re (wrapped in) an expression that evaluates to something that can be rendered.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://mdxjs.com/docs/what-is-mdx/",
        "retrievedAt": "2026-08-26",
        "title": "What is MDX? | MDX"
      }
    ],
    "summary": "An MDX document may contain whole JavaScript programs, so compiling one is executing author-supplied code.",
    "title": "Source: What is MDX? | MDX",
    "type": "evidence"
  },
  {
    "id": "EVD-0026",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"MDX supports import and export statements from JavaScript as well.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://mdxjs.com/docs/what-is-mdx/",
        "retrievedAt": "2026-08-26",
        "title": "What is MDX? | MDX"
      }
    ],
    "summary": "MDX documents may import and export ES modules, so a document can pull in any module the bundler can resolve.",
    "title": "Source: What is MDX? | MDX",
    "type": "evidence"
  },
  {
    "id": "EVD-0027",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"MDX is a programming language. If you trust your authors, that’s fine. If you don’t, it’s unsafe.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://mdxjs.com/docs/getting-started/",
        "retrievedAt": "2026-08-26",
        "title": "Getting started | MDX"
      }
    ],
    "summary": "MDX's own security model is author trust rather than containment; it classifies MDX as a programming language.",
    "title": "Source: Getting started | MDX",
    "type": "evidence"
  },
  {
    "id": "EVD-0028",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"Do not let random people from the internet write MDX.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://mdxjs.com/docs/getting-started/",
        "retrievedAt": "2026-08-26",
        "title": "Getting started | MDX"
      }
    ],
    "summary": "The MDX project advises against letting untrusted parties author MDX and offers no in-language sandbox.",
    "title": "Source: Getting started | MDX",
    "type": "evidence"
  },
  {
    "id": "EVD-0029",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"Expected component `NoteIcon` to be defined: you likely forgot to import, pass, or provide it.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://mdxjs.com/packages/mdx/",
        "retrievedAt": "2026-08-26",
        "title": "@mdx-js/mdx | MDX"
      }
    ],
    "summary": "A reference to a component that was never provided fails at RUNTIME with a named MDX error, not at compile time.",
    "title": "Source: @mdx-js/mdx | MDX",
    "type": "evidence"
  },
  {
    "id": "EVD-0030",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"Note that components must be defined. You can import them, define them locally, or pass them in later\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://mdxjs.com/docs/what-is-mdx/",
        "retrievedAt": "2026-08-26",
        "title": "What is MDX? | MDX"
      }
    ],
    "summary": "MDX requires components to be defined, and names exactly three ways a document may obtain one.",
    "title": "Source: What is MDX? | MDX",
    "type": "evidence"
  },
  {
    "id": "EVD-0031",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"`mdx-components.tsx` is **required** to use `@next/mdx` with App Router and will not work without it.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/guides/mdx",
        "retrievedAt": "2026-08-26",
        "title": "https://nextjs.org/docs/app/guides/mdx"
      }
    ],
    "summary": "With @next/mdx and the App Router, the global component-mapping file is mandatory.",
    "title": "Source: https://nextjs.org/docs/app/guides/mdx",
    "type": "evidence"
  },
  {
    "id": "EVD-0032",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"Adding styles and components in `mdx-components.tsx` will affect *all* MDX files in your application.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/guides/mdx",
        "retrievedAt": "2026-08-26",
        "title": "https://nextjs.org/docs/app/guides/mdx"
      }
    ],
    "summary": "The global MDX component mapping applies to every MDX file in the application, not per document.",
    "title": "Source: https://nextjs.org/docs/app/guides/mdx",
    "type": "evidence"
  },
  {
    "id": "EVD-0033",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"These will merge with and override\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/guides/mdx",
        "retrievedAt": "2026-08-26",
        "title": "https://nextjs.org/docs/app/guides/mdx"
      }
    ],
    "summary": "Per-page components merge with and override the global mapping rather than replacing it.",
    "title": "Source: https://nextjs.org/docs/app/guides/mdx",
    "type": "evidence"
  },
  {
    "id": "EVD-0034",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"The serialize function used to compile MDX in next-mdx-remote is vulnerable to arbitrary code execution due to insufficient sanitization of MDX content.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://api.github.com/advisories/GHSA-g4xw-jxrg-5f6m",
        "retrievedAt": "2026-08-26",
        "title": "https://api.github.com/advisories/GHSA-g4xw-jxrg-5f6m"
      }
    ],
    "summary": "Server-side rendering of untrusted MDX produced a real, GitHub-reviewed high-severity code-execution vulnerability.",
    "title": "Source: https://api.github.com/advisories/GHSA-g4xw-jxrg-5f6m",
    "type": "evidence"
  },
  {
    "id": "EVD-0035",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"\"vulnerable_version_range\":\">= 4.3.0, < 6.0.0\",\"first_patched_version\":\"6.0.0\"\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://api.github.com/advisories/GHSA-g4xw-jxrg-5f6m",
        "retrievedAt": "2026-08-26",
        "title": "https://api.github.com/advisories/GHSA-g4xw-jxrg-5f6m"
      }
    ],
    "summary": "CVE-2026-0969 is classified as code injection at CVSS 8.8, affecting next-mdx-remote up to 6.0.0.",
    "title": "Source: https://api.github.com/advisories/GHSA-g4xw-jxrg-5f6m",
    "type": "evidence"
  },
  {
    "id": "EVD-0036",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"Introduced the parameters `blockJS` and `blockDangerousJS` that controls how JS in interpreted during compiling MDX. Both default to true for security reasons.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://api.github.com/repos/hashicorp/next-mdx-remote/releases/tags/v6.0.0",
        "retrievedAt": "2026-08-26",
        "title": "https://api.github.com/repos/hashicorp/next-mdx-remote/releases/tags/v6.0.0"
      }
    ],
    "summary": "The maintainers' remedy was to BLOCK JavaScript in MDX by default rather than to sanitise it.",
    "title": "Source: https://api.github.com/repos/hashicorp/next-mdx-remote/releases/tags/v6.0.0",
    "type": "evidence"
  },
  {
    "id": "EVD-0037",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"By default, JavaScript expressions (like `{variable}` or `{func()}`) are **disabled for security** for versions 6.0.0 and above.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://raw.githubusercontent.com/hashicorp/next-mdx-remote/main/README.md",
        "retrievedAt": "2026-08-26",
        "title": "https://raw.githubusercontent.com/hashicorp/next-mdx-remote/main/README.md"
      }
    ],
    "summary": "From 6.0.0 onward JavaScript expressions in MDX are disabled by default for security.",
    "title": "Source: https://raw.githubusercontent.com/hashicorp/next-mdx-remote/main/README.md",
    "type": "evidence"
  },
  {
    "id": "EVD-0038",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"which will provide a best effort option to blocks dangerous operations like `eval`, `Function`, `process`, `require`, and other globals that could lead to remote code execution (RCE)\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://raw.githubusercontent.com/hashicorp/next-mdx-remote/main/README.md",
        "retrievedAt": "2026-08-26",
        "title": "https://raw.githubusercontent.com/hashicorp/next-mdx-remote/main/README.md"
      }
    ],
    "summary": "Even with JS enabled, the protective mode is described by its own maintainers as best effort rather than a boundary.",
    "title": "Source: https://raw.githubusercontent.com/hashicorp/next-mdx-remote/main/README.md",
    "type": "evidence"
  },
  {
    "id": "EVD-0039",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"Only set `blockDangerousJS: false` if you completely trust the MDX content source. This removes critical security protections and could allow RCE attacks.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://raw.githubusercontent.com/hashicorp/next-mdx-remote/main/README.md",
        "retrievedAt": "2026-08-26",
        "title": "https://raw.githubusercontent.com/hashicorp/next-mdx-remote/main/README.md"
      }
    ],
    "summary": "Fully unrestricted MDX is documented as safe only for content the operator completely trusts.",
    "title": "Source: https://raw.githubusercontent.com/hashicorp/next-mdx-remote/main/README.md",
    "type": "evidence"
  },
  {
    "id": "EVD-0040",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"plugin **to remove import and/or export statements (`mdxjsEsm`) in MDX documents.**\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://raw.githubusercontent.com/ipikuka/remark-mdx-remove-esm/main/README.md",
        "retrievedAt": "2026-08-26",
        "title": "https://raw.githubusercontent.com/ipikuka/remark-mdx-remove-esm/main/README.md"
      }
    ],
    "summary": "Stripping ESM import/export from MDX at compile time is an established published practice, not a bespoke idea.",
    "title": "Source: https://raw.githubusercontent.com/ipikuka/remark-mdx-remove-esm/main/README.md",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "packageRoot": "docs/plan/",
        "publisher": "lib/handoff/publish.mjs readStageDocs()",
        "repository": "visual-project-workflow",
        "snapshot": "6c86330767dcbbfa"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "recipient-rendering-behaviour",
          "reason": "What a recipient DOES with a package is outside this repository. The package ships document text; whether anyone compiles it, and with which toolchain, cannot be observed from here — which is exactly why the property matters rather than why it does not.",
          "state": "not-observable"
        },
        {
          "fact": "mdx-extension-behaviour",
          "reason": "No `.mdx` file exists under `planning-content/stages/`, so the filter's behaviour on one was read from the source rather than exercised. Running it would need a stage document that does not yet exist.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0041",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ Recorded as an observation of THIS repository because the consequence below is not derivable from any external source, and it is the point at which an ecosystem CVE becomes a fact about this project rather than an interesting story about someone else's.",
    "observedAt": "2026-08-26",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "`lib/handoff/publish.mjs` reads `planning-content/stages/`, filters `readdirSync(dir).filter((f) => f.endsWith(\".md\"))`, and copies each file's text unchanged into the package — the code comments it as \"Stage narratives ship as-is: they are authored planning content, not generated output.\" The published package at `docs/plan/` contains nine such documents under `docs/`, one per stage, at snapshot 6c86330767dcbbfa.",
    "title": "Observation: the handoff package ships every stage document verbatim, and filters on `.md`",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "builder": "Turbopack (16.3.2 default)",
        "framework": "Next.js 16.3.2",
        "integration": "@next/mdx",
        "network": "npm install reached the public registry",
        "node": "v24.18.0",
        "node-version": "v24.18.0",
        "os": "Windows 11",
        "permittedComponents": "Callout",
        "react": "19.2.8",
        "workspace": "one disposable workspace, rebuilt per case, removal verified"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "rejection-completeness",
          "reason": "Five forbidden constructs were exercised: import/export, flow and text expressions, JSX attribute expressions, spread attributes, and unmapped components. That the plugin catches EVERY way to reach JavaScript in MDX was not established and cannot be by example — it needs an argument over the mdast node types, which is why stripping is retained as defence in depth.",
          "state": "not-captured"
        },
        {
          "fact": "rehype-and-recma-stages",
          "reason": "The plugin runs at the remark (mdast) stage only. Whether a later rehype or recma plugin could reintroduce JavaScript was not tested.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0042",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ TWO CONFIGURATION CONSTRAINTS WERE MEASURED, and both first presented as the contract failing. (1) Next.js 16.3.2 builds with TURBOPACK by default, which serialises the config — so a remark plugin must be given as a module path, not a function reference. (2) That path must be ABSOLUTE: `@next/mdx` resolves a relative path with `require.resolve(p, {paths:[projectRoot]})` from its own package directory, so `./lib/plugin.mjs` fails with `Cannot find module` and a require stack rooted in `node_modules/@next/mdx`. That message reads like a broken plugin rather than a configuration rule, and it cost a build to tell apart. ⚠️ REJECT, NOT STRIP, is what makes the diagnostics possible at all. `file.fail` carries the node's position; a stripping plugin has nothing to report because by construction nothing is wrong afterwards.",
    "observedAt": "2026-08-26",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "One `@next/mdx` application, a remark plugin that calls `file.fail` on forbidden nodes, and six documents built through `next build`. accept: built ✓; reject-import: refused @ 1:1 ✓; reject-expression: refused @ 3:8 ✓; reject-attr-expression: refused @ 3:10 ✓; reject-spread: refused @ 3:10 ✓; reject-unmapped: refused @ 3:1 ✓. The accepted document built as a route, rendered its markdown, and rendered the mapped component. Each of the five refusals failed the build and named the file with a line:column position and a reason.",
    "title": "Experiment: restricted MDX rejects forbidden JavaScript at build time, with positions",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "builder": "Turbopack (16.3.2 default)",
        "framework": "Next.js 16.3.2",
        "integration": "@next/mdx",
        "node": "v24.18.0",
        "node-version": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "route": "app/doc/page.mdx",
        "workspace": "disposable, removal verified"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "connection-contract-applied",
          "reason": "DEC-0019's contract was deliberately NOT applied here, because it could not help: the document becomes JavaScript at build time, so there is no request-time read for `connection()` to make dynamic. Measuring it would have confirmed the same value for a different reason and invited the wrong conclusion.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0043",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ FOUND BY LOOKING AT THE ROUTE SYMBOL, not by asking the question. The accepted case built as `○ /doc` — static — which is the symbol AST-0019 is about, and the freshness matrix had already made it worth chasing.",
    "observedAt": "2026-08-26",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "An `app/doc/page.mdx` containing a mapped component built as a static route under `next build`. With `next start` serving it, the file was rewritten with a different marker and re-requested: the served page continued to show the build-time marker.",
    "title": "Experiment: a file-routed MDX document is compiled into the build and does not reflect an edit",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "framework": "@mdx-js/mdx",
        "node": "v24.18.0",
        "os": "Windows 11",
        "outputFormat": "function-body",
        "permittedComponents": "Callout",
        "react": "19.2.8"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "server-component-integration",
          "reason": "Compilation and rendering were exercised directly, not inside a Next.js Server Component behind DEC-0019's `connection()` boundary. Composing the two is the next step and has not been run.",
          "state": "not-captured"
        },
        {
          "fact": "compilation-cost-per-request",
          "reason": "Timing was not collected. Whether compiling a stage document per request is acceptable at the sizes this project produces is unmeasured.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0044",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THIS BECAME LOAD-BEARING ONLY BECAUSE OF THE FRESHNESS FINDING. If file-routed MDX had reflected edits, runtime compilation would never have been needed and this would be a curiosity. As it stands the requirements force runtime compilation, runtime compilation of author-controlled MDX is the exact shape of CVE-2026-0969, and this is the measurement that says the plugin still refuses there.",
    "observedAt": "2026-08-26",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "The identical remark plugin was run through `@mdx-js/mdx`'s `compile` + `run` in a Node process rather than a bundler, against the same six documents, rendering with `renderToStaticMarkup`. accept: compiled; reject-import: refused @ 1:1; reject-expression: refused @ 3:8; reject-attr-expression: refused @ 3:10; reject-spread: refused @ 3:10; reject-unmapped: refused @ 3:1. All six matched the build-time outcomes, and every refusal carried a position and the source path.",
    "title": "Experiment: the same rejection contract holds under runtime MDX compilation",
    "type": "evidence"
  },
  {
    "id": "EVD-0045",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"JavaScript modules can be shared between both Server and Client Components modules. This means it's possible to accidentally import server-only code into the client.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/getting-started/server-and-client-components",
        "retrievedAt": "2026-08-27",
        "title": "https://nextjs.org/docs/app/getting-started/server-and-client-components"
      }
    ],
    "summary": "Modules are shared between server and client, so server-only code can be imported into the client by accident.",
    "title": "Source: https://nextjs.org/docs/app/getting-started/server-and-client-components",
    "type": "evidence"
  },
  {
    "id": "EVD-0046",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"Once a file is marked with `\"use client\"`, **all of its imports and the components it directly renders are included in the client bundle**.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/getting-started/server-and-client-components",
        "retrievedAt": "2026-08-27",
        "title": "https://nextjs.org/docs/app/getting-started/server-and-client-components"
      }
    ],
    "summary": "The client-bundle rule is TRANSITIVE: a `use client` file drags all of its imports into the client bundle.",
    "title": "Source: https://nextjs.org/docs/app/getting-started/server-and-client-components",
    "type": "evidence"
  },
  {
    "id": "EVD-0047",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"Next.js handles `server-only` and `client-only` imports internally to provide clearer error messages when a module is used in the wrong environment. The contents of these packages from NPM are not used by Next.js.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/getting-started/server-and-client-components",
        "retrievedAt": "2026-08-27",
        "title": "https://nextjs.org/docs/app/getting-started/server-and-client-components"
      }
    ],
    "summary": "Next.js implements the server-only guard itself; the npm package's contents are not used.",
    "title": "Source: https://nextjs.org/docs/app/getting-started/server-and-client-components",
    "type": "evidence"
  },
  {
    "id": "EVD-0048",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"In Next.js, installing `server-only` or `client-only` is **optional**.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://nextjs.org/docs/app/getting-started/server-and-client-components",
        "retrievedAt": "2026-08-27",
        "title": "https://nextjs.org/docs/app/getting-started/server-and-client-components"
      }
    ],
    "summary": "Installing the server-only package is optional in Next.js.",
    "title": "Source: https://nextjs.org/docs/app/getting-started/server-and-client-components",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "builder": "Turbopack (16.3.2 default)",
        "framework": "Next.js 16.3.2",
        "layout": "lib/ as a sibling of app/, plain relative import",
        "node": "v24.18.0",
        "node-version": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "workspace": "disposable, rebuilt per case, removal verified"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "runtime-behaviour-of-an-unguarded-client-import",
          "reason": "The build never completed, so there is no server to observe. Whether a different bundler configuration would have produced a running app with a broken client chunk was not explored.",
          "state": "not-observable"
        },
        {
          "fact": "webpack-builder",
          "reason": "Turbopack is the default at 16.3.2 and was the only builder exercised. The unguarded failure mode is a Turbopack internal error and may differ under webpack.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0049",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE CONTRAST IS THE FINDING, not the failure. Both client cases fail, so a probe that only asked 'is it prevented' would have reported success for both and concluded the marker was optional — which is what the Next.js documentation says. What separates them is whether anyone can tell WHY: `TurbopackInternalError` with `location: undefined` is indistinguishable from a bundler bug. ⚠️ MESSAGE ACCURACY CAVEAT, recorded because it would mislead: in the guarded direct-import case Next.js said \"you are using it in the Pages Router\" — this project uses the App Router, and the wording is simply wrong. The adapter case (EVD-0050) produced the correct App Router message. The diagnostic is locatable either way; its prose is not always right.",
    "observedAt": "2026-08-27",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "A `lib/planning.js` using `node:fs`, `node:child_process` and a lock-shaped API, imported four ways under `next build`. Imported by a Server Component it built and rendered the value, with and without a `server-only` marker. Imported by a `'use client'` component it failed both times — but differently: UNGUARDED the build failed with `Error: Failed to write app endpoint /page`, `type: 'TurbopackInternalError'`, `location: undefined`, naming no file; GUARDED it failed at `./lib/planning.js:1:1` with the offending source line, a caret, an explanation and a docs link.",
    "title": "Experiment: a Node-only module across the Server/Client boundary, guarded and unguarded",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "builder": "Turbopack (16.3.2 default)",
        "framework": "Next.js 16.3.2",
        "guardLocation": "app-side adapter, not lib/",
        "layout": "lib/ as a sibling of app/, plain relative import",
        "node": "v24.18.0",
        "node-version": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8",
        "workspace": "disposable, removal verified"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "adapter-completeness",
          "reason": "One adapter re-exporting two functions was exercised. Whether every module the shell needs from lib/ can be re-exported this way — some take locks, spawn processes, or hold module-level state — was not tested, and the answer may differ per module.",
          "state": "not-captured"
        },
        {
          "fact": "enforcement-that-app-code-never-imports-lib-directly",
          "reason": "The adapter is a convention here, not a checked rule. Nothing in this probe stops a component importing `../../lib/planning.js` and bypassing the guard entirely. That is REQ-0021's territory and it is unbuilt.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0050",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE PLAIN-NODE CHECK IS THE POINT OF THIS PROBE. `server-only` resolves `react-server` to `empty.js` only inside the RSC compiler; everywhere else it resolves to `index.js`, which THROWS. Measured directly: importing it under bare Node throws \"This module cannot be imported from a Client Component module.\" So the obvious placement — mark `lib/` and be done — would break every CLI in `bin/` and the entire test suite, which import `lib/` under plain Node.",
    "observedAt": "2026-08-27",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "`lib/planning.js` carries no marker and stays importable by plain Node — verified by importing it in a bare `node -e` process, which returned the artifact value. An `app/server/planning.js` adapter carries `import 'server-only'` and re-exports from it. A Server Component importing the adapter built and rendered the value. A `'use client'` component importing the adapter FAILED the build at `./app/server/planning.js:1:1`, quoting the import line and the re-export beneath it, with the correct App Router message: the module is a Client Component because one of its parents is marked `use client`.",
    "title": "Experiment: the `server-only` guard on an app-side adapter, with `lib/` left unmarked",
    "type": "evidence"
  },
  {
    "id": "EVD-0051",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"it does not attempt to reconnect.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://html.spec.whatwg.org/multipage/server-sent-events.html",
        "retrievedAt": "2026-08-27",
        "title": "HTML Standard"
      }
    ],
    "summary": "Once an SSE connection is FAILED (as opposed to merely dropped), the client never retries.",
    "title": "Source: HTML Standard",
    "type": "evidence"
  },
  {
    "id": "EVD-0052",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"unless the user agent knows that to be futile, in which case the user agent may fail the connection\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://html.spec.whatwg.org/multipage/server-sent-events.html",
        "retrievedAt": "2026-08-27",
        "title": "HTML Standard"
      }
    ],
    "summary": "A network error reconnects, but the user agent may give up permanently if it judges retrying futile.",
    "title": "Source: HTML Standard",
    "type": "evidence"
  },
  {
    "id": "EVD-0053",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"s status is not 200, or if res 's ` Content-Type ` is not ` text/event-stream `, then fail the connection\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://html.spec.whatwg.org/multipage/server-sent-events.html",
        "retrievedAt": "2026-08-27",
        "title": "HTML Standard"
      }
    ],
    "summary": "A non-200 status or a wrong Content-Type FAILS the connection, which is the terminal state, not a retry.",
    "title": "Source: HTML Standard",
    "type": "evidence"
  },
  {
    "id": "EVD-0054",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"is not a network error , then reestablish the connection\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://html.spec.whatwg.org/multipage/server-sent-events.html",
        "retrievedAt": "2026-08-27",
        "title": "HTML Standard"
      }
    ],
    "summary": "A stream that ends without a network error is reconnected automatically.",
    "title": "Source: HTML Standard",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "node": "v24.18.0",
        "node-version": "v24.18.0",
        "os": "Windows 11",
        "substrate": "app/server.mjs walking skeleton",
        "transport": "text/event-stream over http",
        "watcher": "chokidar",
        "workspace": "copy of planning-content in a disposable directory, removal verified"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "browser-client-reconnect-behaviour",
          "reason": "There is no global `EventSource` in this Node build and no browser on this host. Writing a polyfill and measuring it would have measured my own code while implying a browser had been observed. The client half is source-supported only, from the WHATWG specification (EVD-0051..EVD-0054).",
          "state": "not-observable"
        },
        {
          "fact": "idle-timeout-beyond-six-seconds",
          "reason": "The idle stream was observed for six seconds. Whether an intermediary or the OS would drop a longer-idle connection was not exercised, and it is the case a heartbeat would exist to survive.",
          "state": "not-captured"
        },
        {
          "fact": "concurrent-clients",
          "reason": "One reader at a time. Fan-out behaviour across several open pages was not measured.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0055",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE SERVER HALF IS MEASURABLE AND THE CLIENT HALF IS NOT, and the split is stated here so nothing downstream reads a browser claim into this record. What a reconnecting client can RECOVER is a property of what the server sent, and that is what was measured.",
    "observedAt": "2026-08-27",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "The skeleton was started against a copy of this project's planning content and its `/events` stream read directly over HTTP. The opening frame is exactly \"retry: 500\\n\\n\" — a reconnection time and nothing else; no `id:` field is ever sent. A change made while connected was delivered as `data: changed`. A change made during a disconnected window was NOT delivered on reconnect. An idle stream emitted ZERO bytes over six seconds after the opening frame. A write after a client had left did not disturb the server.",
    "title": "Experiment: the walking skeleton's server-side SSE contract",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "corruption": "one assertion file replaced with invalid JSON",
        "node": "v24.18.0",
        "node-version": "v24.18.0",
        "os": "Windows 11",
        "substrate": "app/server.mjs walking skeleton",
        "transport": "text/event-stream over http",
        "watcher": "chokidar",
        "workspace": "disposable copy, removal verified"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "other-malformation-modes",
          "reason": "One mode was exercised — syntactically invalid JSON. Schema-valid-but-wrong content, an unreadable file, and a directory in place of a file were not tested and may behave differently.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0056",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE COUNT IS THE PART THAT MAKES THIS BAD. A view that quietly omitted a broken artifact would merely be incomplete. This one omits it AND prints a total that still includes it, so the page actively asserts the artifact is present. An operator has no reason to look for what is missing.",
    "observedAt": "2026-08-27",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "With the skeleton serving a copy of this project's content, the page reported 178 artifacts and rendered AST-0001. One artifact file was then replaced with invalid JSON. The next request returned HTTP 200 with a normally-formed page, the header still reported 178 artifacts, the number of lint findings shown was unchanged, and AST-0001 was absent from the page. No error, no warning, and no indication that anything had failed to load.",
    "title": "Experiment: a malformed artifact is dropped from the view while the count still includes it",
    "type": "evidence"
  },
  {
    "id": "EVD-0057",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"If the line starts with a U+003A COLON character (:) Ignore the line.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://html.spec.whatwg.org/multipage/server-sent-events.html",
        "retrievedAt": "2026-08-27",
        "title": "HTML Standard"
      }
    ],
    "summary": "A comment line in an SSE stream is IGNORED by the client parser, so it fires no JavaScript event and cannot be observed by page code.",
    "title": "Source: HTML Standard",
    "type": "evidence"
  },
  {
    "id": "EVD-0058",
    "kind": "source",
    "lifecycle": "active",
    "notes": "Quoted from the page, verified present at retrieval time: \"To protect against such proxy servers, authors can include a comment line (one starting with a ':' character) every 15 seconds or so.\" ⚠️ The full document was NOT retained — there is no capture store yet (7b builds capture plans). What survives is the locator, the retrieval date and the verified quote.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "sources": [
      {
        "locator": "https://html.spec.whatwg.org/multipage/server-sent-events.html",
        "retrievedAt": "2026-08-27",
        "title": "HTML Standard"
      }
    ],
    "summary": "Comment lines exist to keep proxies from dropping the connection — a transport keepalive, not a client-visible signal.",
    "title": "Source: HTML Standard",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "builder": "Turbopack (16.3.2 default)",
        "framework": "Next.js 16.3.2",
        "layout": "app/ at the repository root, adapters at app/server/",
        "mode": "next build",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "webpack-builder",
          "reason": "Turbopack is the default at 16.3.2 and was the only builder exercised. AST-0032 measured the UNGUARDED failure mode as a Turbopack internal error, and whether webpack reports it differently was not tested here either.",
          "state": "not-captured"
        },
        {
          "fact": "indirect-reexport-chains",
          "reason": "The client violation imported an adapter directly. A chain that reaches `lib/` through an intermediate module was not exercised — that is the bypass path TSK-0006's static check is required to detect, and it is not this task's.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0059",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE ROUTE TABLE CAUGHT A SILENT FAILURE AGAIN, on the second consecutive task. The probe was first placed at `app/_probe/`, the build succeeded, and the route never appeared: a leading underscore marks a PRIVATE FOLDER in the App Router and opts the directory out of routing. `✓ Compiled successfully` was printed either way. TSK-0003 hit the same class through `src/app/` precedence; the shape is 'a build that succeeds while rendering nothing you wrote', and only the route table distinguishes them. ⚠️ The wording of the client-side diagnostic is deliberately NOT recorded as a required string. ACC-0027 forbids asserting it, because Next.js produced two different messages for this violation reached two ways and one of them named the wrong router.",
    "observedAt": "2026-08-27",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Two adapters were built over `lib/` — `content.js` and `stages.js` — each beginning with `import \"server-only\"` and re-exporting by explicit name. A Server Component importing both compiled, appeared in the route table as `○ /probe-adapters`, served HTTP 200, and reported every imported binding resolved to a real value (`function,object,function,function`). A `'use client'` module importing one of them FAILED the build at `./app/server/content.js:1:1`, quoting the offending source line with a caret and printing the import chain from the page through the client module to the adapter; `next build` exited 1 with the violation present and 0 once it was removed. Both probe routes were deleted afterwards and the tree rebuilt clean.",
    "title": "Experiment: the `app/server/` adapter door, verified in both directions",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "builder": "Turbopack (16.3.2 default)",
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "independent-suspense-boundaries",
          "reason": "The reads are separate exports, which is the SHAPE independent boundaries require, but no view puts them behind boundaries yet — the stage view is TSK-0009. ACC-0016 therefore stays not-evaluated: the thing it measures does not exist.",
          "state": "not-captured"
        },
        {
          "fact": "freshness-after-an-external-write",
          "reason": "The route was measured as dynamic, not measured serving changed content after a write while running. That is what the route symbol predicts and it was not confirmed here.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0060",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ `ƒ` IS THE OBSERVABLE THAT MATTERS, and it is the contract showing up in the build output: `connection()` made the route dynamic, so nothing it reads can be baked into the build. `/` staying `○` in the same build is the control — the difference is the contract, not the project.",
    "observedAt": "2026-08-27",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "The reader was exercised from a temporary Server Component route under `next build` + `next start`. The route built as `ƒ /probe-read` — DYNAMIC — while `/`, which reads nothing, stayed `○` static. All three reads returned live project data: nine stages with `currentStage` null (every gate ready), 236 artifacts, 0 lint findings, 36 assertions; `06-risk-feasibility` ready with two criteria, the first `high-severity-risks-mitigated` satisfied; and the stage document read back as `06-risk-feasibility.md`, 10590 bytes. The probe route was deleted and the tree rebuilt clean.",
    "title": "Experiment: the application reader under a production build",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "analysis": "static, text only; no application code executed",
        "node": "v24.18.0",
        "os": "Windows 11"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "computed-dynamic-imports",
          "reason": "`import(someVariable)` cannot be followed by a text analysis. It is DETECTED and reported as an unfollowable import, and the lint exits non-zero on one, so the blind spot is loud rather than silent — but a specifier that is computed cannot be resolved without running the code.",
          "state": "not-observable"
        },
        {
          "fact": "non-relative-alias-imports",
          "reason": "Only relative specifiers are resolved; a bare specifier is treated as a package. If a path alias (`@/lib/...`) is ever configured, the analyser would treat it as external and miss it. No alias is configured today.",
          "state": "not-captured"
        },
        {
          "fact": "read-and-suspense-confinement",
          "reason": "This task covers the import boundary only. The planning-read and `<Suspense>` half is TSK-0015, and ACC-0024 needs both.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0061",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE FIXTURES CAUGHT TWO REAL BUGS BEFORE THE CHECK WAS TRUSTED, which is the argument for writing them first. (1) Every violation was reported ONE LINE EARLY: the pattern consumes the character before `import`/`export` so a specifier inside an identifier cannot match, and that character is usually the previous line's newline. The transitive fixture caught it because its violation is deliberately not on line 1. (2) A lazy `[\\s\\S]*?` let a side-effect `import \"server-only\";` run on to the NEXT statement's `from` clause, attributing that specifier to the wrong line and swallowing the statement between them. ⚠️ A THIRD DEFECT WAS FOUND BY RUNNING IT, not by a fixture: the first version scoped the check to modules REACHABLE from a route, and since no view imports the reader yet it walked two files and reported the project clean without looking at the reader or either adapter. Scope is now every module under `app/`; reachability only supplies the chain.",
    "observedAt": "2026-08-28",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "A static analyser was built that reads application modules as text and never executes them. Run against three fixtures: the ALLOWED case (a page reaching `lib/` through an adapter) reported no violations while confirming the adapter was actually walked; the DIRECT case (a page importing `lib/` itself) was reported at `app/page.js:1`; the TRANSITIVE case (a page importing an innocent-looking helper that re-exports `lib/`) was reported at `app/_helpers/shim.js:2` — the file whose import statement is the violation — with the chain `app/page.js → app/_helpers/shim.js`. Run against the real project it reports no violations over 5 scanned modules. Falsified in real application code twice: a direct `lib/` import added to the reader was reported at `app/_read/planning.js:3` with exit 1, and a transitive one through a new `app/_helpers/shim.js` was reported at that file's line 1, also exit 1.",
    "title": "Experiment: the import-boundary check, falsified against fixtures and against real code",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "analysis": "static, text only; no application code executed",
        "node": "v24.18.0",
        "os": "Windows 11"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "suspense-enclosure-in-the-real-application",
          "reason": "No module in the real application consumes the reader yet — the lint reports `0 reader consumer(s)`. The two boundary detectors therefore have nothing to examine there and were falsified against temporary real-code violations and against fixtures instead. They begin covering real code at TSK-0007.",
          "state": "not-observable"
        },
        {
          "fact": "jsx-enclosure-across-files",
          "reason": "A component's enclosure is judged where it is RENDERED by name. A component passed as a prop, rendered through a variable, or wrapped by a boundary in a parent layout rather than at the call site would not be judged correctly. No such pattern exists yet; it is a real limit rather than a hypothetical one.",
          "state": "not-captured"
        },
        {
          "fact": "computed-dynamic-imports",
          "reason": "Unchanged from the import half: a computed specifier cannot be resolved by text, and the lint exits non-zero on one rather than passing over it.",
          "state": "not-observable"
        }
      ]
    },
    "id": "EVD-0062",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE FIXTURES CAUGHT THREE DEFECTS, TWO OF THEM IN THE FIXTURES THEMSELVES. (1) The fixture components imported the reader with `../_read/planning` from `app/`, which climbs OUT of the app directory — so nothing resolved, `consumers` was empty, and the ALLOWED case reported clean for the worst possible reason. The `consumers.length > 0` assertion is what caught it, and it was written for exactly that. (2) A `sed` fix then broke the indirect fixture's path in the other direction. (3) In the analyser, two `new RegExp` template literals lost a backslash level, so `await\\s+` became `awaits+` and matched nothing — a detector that silently could not fire. ⚠️ FIXTURES ARE `.jsx` BECAUSE `node --test` COLLECTS `.js` UNDER `test/`, and these contain JSX that Node cannot parse. Collected, they failed the suite for a reason unrelated to what they test. `.jsx` sits outside the runner's patterns and inside the analyser's.",
    "observedAt": "2026-08-28",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "A second static analysis was added: it scans every module under `app/`, asserts the designated reader exists and was examined, and emits four kinds of violation. Six fixtures, one per detected violation plus an allowed control: ALLOWED reported clean with a consumer present; DIRECT caught a component importing an adapter at line 1; INDIRECT caught a helper re-exporting the adapter at its own line 1, not at the page that used it; MISSING-BOUNDARY caught `<Panel>` rendered bare at the rendering site, line 3; FS-DIRECT caught `node:fs` at line 1; ROUTE-READ caught a route entry awaiting the reader at line 3. Each of the four detectors was then falsified again against a TEMPORARY violation inserted into the real application, and each reported the real file and line before being reverted. A missing reader is reported as `readerSeen: false` rather than as a clean project.",
    "title": "Experiment: read and `<Suspense>` confinement, four detectors falsified twice over",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "builder": "Turbopack (16.3.2 default)",
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "push-based-refresh",
          "reason": "The page was re-requested by hand. Whether a change reaches an OPEN page without a request is the change stream's job (TSK-0011, TSK-0017) and nothing here shows it.",
          "state": "not-captured"
        },
        {
          "fact": "counts-lint-and-malformed-content",
          "reason": "Deliberately not built in this task — they are TSK-0016's, and they are the half of this view that fails silently. Nothing about them was rendered, so nothing about them was measured.",
          "state": "not-observable"
        }
      ]
    },
    "id": "EVD-0063",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ `ƒ` VERSUS `○` IS THE OBSERVABLE THAT MATTERS. Before this task `/` read nothing and built static; it now builds dynamic because the read awaits `connection()`. A route that had been frozen into the build would have kept serving `none` after the file changed — which is exactly the AST-0019 failure, and exactly what DEC-0019's contract exists to prevent. ⚠️ THE REVERT IS PART OF THE MEASUREMENT. A page that changed once could have been showing anything; changing back on the way out is what makes it a function of the file.",
    "observedAt": "2026-08-28",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "The project view was built with `next build` and served with `next start` on loopback. The route built as `ƒ /` — dynamic — where it had been `○` static before it read anything. Served initially it reported `data-vpw-current=\"none\"` and `data-vpw-stages=\"9\"`, showing every gate ready. One stage attestation was then changed on disk through the typed tool — `04-requirement-gaps` / `every-blocking-gap-decided` set to `not-satisfied` — with the server left running and nothing rebuilt. Three seconds later the same URL reported `data-vpw-current=\"04-requirement-gaps\"`, rendered NOT READY, and named the blocking criterion. Reverting the file returned the page to `data-vpw-current=\"none\"`.",
    "title": "Experiment: the running application follows an attestation change on disk",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "compiler": "@mdx-js/mdx 3.1.1 (pinned exactly)",
        "framework": "Next.js 16.3.2",
        "mode": "node, direct compile",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "other-extensions",
          "reason": "Only `.md` and an explicit `format` were exercised. Whether a pathless or unusual extension behaves as a third case was not tested; the fix removes the dependency on the path entirely.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0064",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE DANGEROUS HALF IS THE SECOND ONE. A component quietly rendering as a paragraph is visible to anyone looking at the page. An EXPRESSION COMPILING WITH NO REJECTION is not visible to anyone: the plugin runs, finds no expression nodes because the parser never produced any, and reports success. The entire restricted-MDX contract becomes vacuous, silently, decided by a filename. ⚠️ IT HAD ALREADY MADE A TEST LIE. The suite's 'every REAL stage document compiles under the restricted contract' case passed on all nine documents before the format was pinned — not because they are contract-clean, but because they are `.md` and were never subjected to the rules. They do pass with `format: \"mdx\"`, which is why the finding cost nothing beyond the measurement.",
    "observedAt": "2026-08-28",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Compiling `# Doc\\n\\n<Callout>MARKER</Callout>` through the restricted plugin with `path: \"x.md\"` and no `format` option produced `<h1>Doc</h1><p>MARKER</p>` — the mapped component rendered as a bare paragraph. Compiling `Inline {1 + 1} expression.` the same way COMPILED SUCCESSFULLY with no rejection. With `format: \"mdx\"` passed explicitly, the same two inputs produced the mapped component's `<aside data-vpw-mdx=\"Callout\">` and a refusal naming the file and line:column respectively.",
    "title": "Experiment: `@mdx-js/mdx` infers plain markdown from a `.md` path, and the contract goes silent",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "compiler": "@mdx-js/mdx 3.1.1 (pinned exactly)",
        "contentRoot": "a disposable copy; the project's own content was not touched",
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "compilation-cost",
          "reason": "ACC-0020's protocol was not run here — no warm-up, no sample, no timing. The budget remains an unmeasured target and TSK-0014 owns it.",
          "state": "not-captured"
        },
        {
          "fact": "server-log-contents",
          "reason": "The server's stdout was discarded. The diagnostic is presumed to be there because the compiler throws it, but presuming is why this is recorded as an omission rather than as a finding.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0065",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE REJECTION IS LOCATABLE TO A DEVELOPER AND INVISIBLE TO AN OPERATOR, which is the finding worth carrying forward. `StageDocumentRejected` carries the path, line and column; React's production error boundary replaces all of it with a digest, and the response is a 200 because the shell had already streamed. A stage view that simply renders `<Content />` would show a blank region and a success status for a document the compiler refused precisely. ⚠️ That is DEC-0022's rule arriving in a new place: the operator is looking at a browser, and a failure that reaches only the server log leaves the page looking fine. Catching `StageDocumentRejected` and rendering it is CMP-0016's work (TSK-0009), and it is now a measured obligation rather than a nicety.",
    "observedAt": "2026-08-28",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "The compiler was exercised from a temporary Server Component route under `next build` + `next start`, serving against a copy of `planning-content`. The route built as `ƒ /probe-mdx`. A stage document containing `<Callout>MARKER_ONE</Callout>` rendered the mapped component as `<aside data-vpw-mdx=\"Callout\">`. Editing that document on disk to MARKER_TWO, with nothing rebuilt and the server left running, made the next request return MARKER_TWO and the edited heading. Replacing the document with `Inline {1 + 1} expression.` made the next request return HTTP 200 whose Suspense slot resolved to a React error digest only — no message, no path, no position anywhere in the response.",
    "title": "Experiment: request-time recompilation, and what a rejected document looks like in production",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "compiler": "@mdx-js/mdx 3.1.1 (pinned exactly)",
        "contentRoot": "a disposable copy; the project's own content was not touched",
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "unknown-stage-status-code",
          "reason": "`notFound()` is called inside a Suspense child, so the status is already committed when the stage turns out not to exist — the response is 200 carrying the not-found UI. Returning 404 would mean validating before streaming, which means reading in the route entry, which the boundary check refuses. The failure is visible; the status is not available to make truthful.",
          "state": "not-observable"
        },
        {
          "fact": "review-write",
          "reason": "No write capability exists yet by design: DEC-0021 reviews locking capabilities one at a time and TSK-0012 owns the first. The panel shows identity and status and says the action is unavailable.",
          "state": "not-captured"
        },
        {
          "fact": "slow-read-independence",
          "reason": "Independence was observed because the document read is genuinely slower than the criteria read, not because either was artificially delayed. A deliberate delay would prove it on demand; nothing here forces the ordering, so the observation is real but not reproducible on command.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0066",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE STAGE ID NEVER BECOMES A PATH. It is matched against the stage definitions by object key and against document names by exact comparison over a directory listing, so a traversal-shaped segment is simply an id that matches nothing. There is no sanitising step, which is why there is no sanitising step to get wrong. ⚠️ THE ESCAPED DIAGNOSTIC IS THE PART WORTH KEEPING. The message quotes agent-authored source; rendered as markup it would let a refused document inject into the page reporting its refusal, turning the safety mechanism into the delivery route.",
    "observedAt": "2026-08-28",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "`/stage/[stageId]` built as `ƒ` and was served against a copy of `planning-content`. A valid stage returned 200 with all three regions resolved: `data-vpw-criteria=\"06-risk-feasibility\"` with both criterion markers, `data-vpw-document=\"06-risk-feasibility.md\"`, and `data-vpw-review=\"AST-0021\"` showing `draft`. The same response also carried `data-vpw-loading=\"document\"` — the criteria region had resolved while the document region was still showing its own fallback, which is the two boundaries resolving independently, observed rather than inferred. Replacing the document with `<Danger>` produced 200 with `data-vpw-document=\"rejected\"`, `data-vpw-rejection-at=\"planning-content/stages/06-risk-feasibility.md:3:1\"`, the diagnostic escaped as `&lt;Danger&gt;`, and both criterion markers still present. Replacing it with a permitted `<Callout>` rendered `data-vpw-mdx=\"Callout\"` and an `<h1>`. An unknown stage id, and two traversal-shaped ids, each returned 200 carrying Next.js's visible not-found UI.",
    "title": "Experiment: the stage view — three boundaries, a refused document, and an unknown stage",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "client": "fetch — no cookies, no storage, no shared session",
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "browser-restore-behaviour",
          "reason": "No browser was available. What was measured is that the SERVER derives both selections from the URL and nothing else — which is the property that makes a browser restore work, but is not the same as watching one do it.",
          "state": "not-observable"
        },
        {
          "fact": "client-held-transient-state",
          "reason": "There are no client components yet, so the distinction DEC-0023 draws between transient and correctness-critical state has not been exercised. A static test now guards the first one that appears.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0067",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE CONTROL IS WHAT MAKES THIS A TEST. Asserting that a URL returns the selections it names passes trivially on a page that always shows the same thing; the second URL is what rules that out. Falsified by hard-coding the artifact id in the page — the control fails immediately. ⚠️ AN UNKNOWN ARTIFACT SELECTS NOTHING, which is the clause worth having separately: silently falling back to a valid artifact would be a page confidently showing the wrong record's review status.",
    "observedAt": "2026-08-28",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Against a production build serving a copy of `planning-content`, three fetches with no cookies and no shared state — each a fresh session by construction. `/stage/06-risk-feasibility?artifact=AST-0021` reported stage `06-risk-feasibility` and artifact `AST-0021`; the identical URL fetched again reported the same pair; and `/stage/01-intake?artifact=AST-0022` reported `01-intake` and `AST-0022`, which is the control against a page that ignores its URL. `?artifact=NOPE-9999` produced `data-vpw-review=\"unknown\"`, echoed the id back, selected no other artifact, and left the criteria region rendered.",
    "title": "Experiment: both selections are recoverable from the URL alone",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "contentRoot": "a disposable copy; the project's own content was not touched",
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "non-json-unreadability",
          "reason": "Only invalid JSON was exercised. A file that is unreadable for another reason — permissions, a directory in place of a file, an encoding fault — reaches the same code path but was not tested, and would report whatever message the read threw rather than a line and column.",
          "state": "not-captured"
        },
        {
          "fact": "many-simultaneous-failures",
          "reason": "One corrupt file at a time. The panel renders a list and the count is per type, so several should behave the same way, but that is reasoning rather than measurement.",
          "state": "not-captured"
        },
        {
          "fact": "lint-findings-under-corruption",
          "reason": "The lint region was asserted to still RENDER while a file was corrupt, not that its findings are correct in that state. What the lint reports about a project with an unparsable artifact is the lint's business and is surfaced unchanged (#47).",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0068",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE ORACLE IS WHAT MAKES THIS A TEST. The original ACC-0014 asked that each displayed total equal 'the number rendered on the page' — and the page renders TOTALS, not entries, so it compared a number with itself and would have passed on an implementation printing a constant. Both halves here count parsable files independently of the application. ⚠️ FALSIFIED THE TWO WAYS THAT MATTER. A constant total fails the oracle comparison and the decrement assertion; suppressing the unreadable region fails the located-error assertion — and each fails only its own, which is what stops one check standing in for the other. ⚠️ A SECOND INSTANCE OF AST-0035 WAS FIXED HERE, in this project's own reader rather than in the skeleton: `artifactCount` was `lint.records.length`, which counts FILES including ones that failed to parse. It now counts documents. The defect had been sitting in the reader since TSK-0005 and had never been displayed.",
    "observedAt": "2026-08-28",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Two measurements. OFFLINE, against a fixture whose composition was declared in the test — three requirements, two assertions, one decision — the counting rule returned exactly that; corrupting one requirement returned two requirements with every other type unchanged, and left the bad file visible with a parse error carrying a line and column. IN PRODUCTION, against a copy of `planning-content` served by a real build: the displayed assertion total equalled the number of parsable assertion files counted independently outside the application; corrupting one file with the server running dropped that total by exactly one, left every other type unchanged, rendered `data-vpw-unreadable-at=\"planning-content/data/assertions/…json:1:3\"`, and kept the stages and lint regions rendered; restoring the file returned the total and cleared the error state, all without a rebuild or a restart.",
    "title": "Experiment: totals against an oracle, and a corrupted artifact reported and recovered live",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "contentRoot": "a disposable copy; the project's own content was not touched",
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "node": "v24.18.0",
        "os": "Windows 11",
        "watcher": "chokidar, imported dynamically"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "real-watcher-failure",
          "reason": "A watcher error was injected, not provoked. A real one needs the OS to run out of watch descriptors or a directory to vanish under the process, neither of which can be asked for on demand — which is exactly why the collaborator is injectable.",
          "state": "not-observable"
        },
        {
          "fact": "client-behaviour",
          "reason": "This is the SERVER half. Whether a browser's EventSource fires a listener for these frames, and what a watchdog does when they stop, is TSK-0017's and ACC-0029/ACC-0030 remain not-evaluated.",
          "state": "not-captured"
        },
        {
          "fact": "long-lived-connections",
          "reason": "The production stream was read for seconds, not hours. Whether an idle connection survives a long period, and whether subscriber cleanup holds under many connect/disconnect cycles, was not exercised.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0069",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE INJECTION IS WHAT MAKES THE FAILURE PATH TESTABLE AT ALL. `ENOSPC: watchers exhausted` is not something a test can cause; without an injectable watcher the most important behaviour in this component — that a dead watcher tells every page rather than only the log — would have been asserted by reading the code. ⚠️ THE PRODUCTION CHECK READS THE STREAM INCREMENTALLY AND STOPS. An event stream never finishes, so `await res.text()` would hang until the timeout and report a working stream as a failure. The reader is released as soon as both frames have been seen. ⚠️ FALSIFIED THREE WAYS, each failing only its own assertion: replacing the named heartbeat with a comment keepalive fails the production check; logging a watcher failure instead of broadcasting it fails the failure test; tearing the watcher down on the first disconnect fails the shared-watcher test.",
    "observedAt": "2026-08-28",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Nine tests against the shipping service with the watcher and timers injected, so failure, heartbeat, disconnect and cleanup are decided rather than waited for. Measured: nothing starts until the first subscription (no watcher, no timer); a heartbeat is a NAMED event carrying `{ok:true}`; two subscribers both receive a change hint and a late subscriber receives no replay; one subscriber leaving clears its own timer and leaves the shared watcher running for the other; the last one leaving closes it; a watcher `error` broadcasts a named `failure` event with the reason AND closes every stream, leaving no timer; a subscriber whose send throws is dropped without affecting the rest; and `close()` ends every stream, releases the watcher and refuses further subscriptions. IN PRODUCTION, against a real build serving a temporary content root, an event-stream client read `/events` incrementally and observed `event: heartbeat` with `data: {\"ok\":true}`, then `event: change` after a real file write, with no `id:` field anywhere.",
    "title": "Experiment: the change-stream server, deterministic under injection and live in production",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "eventSource": "a fake driven by the test; no browser was involved",
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "node": "v24.18.0",
        "os": "Windows 11",
        "react": "19.2.8"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "browser-behaviour",
          "reason": "No browser was available in this environment. What a real EventSource does on reconnect — its own backoff, whether it fires `open` again rather than constructing afresh, how quickly it notices a dropped connection — was NOT measured. ACC-0029 and ACC-0030 are about what a PAGE does and remain not-evaluated.",
          "state": "not-observable"
        },
        {
          "fact": "hydration",
          "reason": "The check proves the component's code is inside a script the page loads. That the script then executes, hydrates and opens a connection follows from it but was not watched happening.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0070",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE HARD PART IS TIMING, AND TIMING IS NOT TESTABLE INSIDE A HOOK. The logic lives in `app/_stream/logic.js` taking EventSource and its timers as arguments; the component is the thin part around it. Every distinction that could be got wrong — connection versus reconnection, a deadline that must reset, a reload that must not cause another — is decided by a test rather than waited for. ⚠️ IT LIVES IN `app/`, NOT `lib/`, BECAUSE THE BOUNDARY CHECK SAID SO. The first draft put it beside the server half in `lib/` and `lint:shell` refused the import immediately. It could not have gone through `app/server/*` either: those carry `server-only`, which throws in a client component by design. REQ-0021's check located a real design mistake rather than an inconvenience. ⚠️ FALSIFIED: a static stand-in in the layout — the same markup and the same words, with no client component behind it — passes both HTML assertions and fails only the script assertion, which is the exact failure the check exists for: an indicator that renders once and then reassures permanently while nothing can refresh the page.",
    "observedAt": "2026-08-28",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Thirteen tests against the shipping watchdog logic with EventSource and timers injected, so timing is decided rather than waited for. Measured: the FIRST open goes live and does NOT reload, a reconnection after trouble DOES; an open with nothing wrong in between does not, so the rule is not merely 'reload on every open after the first'; each named heartbeat resets the deadline and restores live; expiry goes stale while the socket is still open; a watcher failure goes stale at once and stops waiting; three change hints reload once; start() twice makes one EventSource; stop() closes it, clears the timer and refuses to restart, and a hint arriving afterwards does not navigate. IN PRODUCTION, against a real build, the served page carries the indicator markup AND a script the page loads that contains the component's own state text.",
    "title": "Experiment: the change-stream client, deterministic under injection and shipped to the browser",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "cpu": "AMD Ryzen AI MAX+ 395 w/ Radeon 8060S, 32 cores",
        "document": "02-intent-decomposition.md, 21047 bytes, the largest at time of evaluation",
        "framework": "Next.js 16.3.2",
        "harness": "bin/bench-compile.mjs",
        "mdx": "@mdx-js/mdx 3.1.1",
        "memory": "27.6 GB",
        "mode": "next build + next start",
        "node": "v24.18.0",
        "os": "Windows 11, win32 x64",
        "protocol": "10 warm-up discarded, 50 serial measured, concurrency 1, p95 nearest-rank"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "ambient-load",
          "reason": "The protocol pins the host, the document, the warm-up and the sample, but says only 'no other significant load'. Nothing measured what else the machine was doing, and the runs were taken back to back with a full production build immediately before each. That is the most likely source of a 50% spread, and it is NOT controlled by the protocol as written.",
          "state": "not-captured"
        },
        {
          "fact": "read-cost-attribution",
          "reason": "The read was measured as one span. Which part of it dominates — reading the artifact files, validating them against schemas, or the lint pass over them — was not separated, so the obvious optimisation is not yet identified, only its location.",
          "state": "not-captured"
        },
        {
          "fact": "other-documents",
          "reason": "Only the largest document was measured, as the protocol specifies. The read cost is per-request and appears independent of document size, so smaller documents likely cost nearly the same — but that was not measured.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0071",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE PRESCRIBED REMEDY DOES NOT ADDRESS THE MEASURED COST, AND THIS IS THE FINDING. DEC-0020 and ACC-0020 both record that if the budget is missed by a wide margin the answer is that decision's second alternative — plain markdown for stage documents — rather than a larger number. The measurement says that would remove 15-29 ms from a 245-375 ms span. The restricted MDX contract would be given up, its rejection guarantee with it, and the budget would still be missed in the same three runs. The cost is in `readStageDocument`, and WHAT inside it is not yet known. ⚠️ AN EARLIER VERSION OF THIS RECORD MISATTRIBUTED THAT SPAN TO A PER-REQUEST READ OF ALL 253 ARTIFACTS. That is wrong and the correction matters, because it was the wrong answer to the right question. `readStageDocument` awaits `connection()`, lists the stage directory and reads the nine stage documents; it does NOT call `lintProject`. The 234-359 ms therefore combines the `connection()` wait with that directory listing and those nine reads, and nothing here separates them. The cause is UNKNOWN, not merely unoptimised. ⚠️ THE VERDICT IS NOT STABLE, SO A SINGLE RUN IS NOT A RESULT. Three passes and three misses from an identical protocol on an idle-looking host, spread 245-375 ms. Reporting the first run alone would have recorded a pass; reporting the fourth alone would have recorded a 25% miss. Both would have been true of that run and useless as a judgement. ⚠️ VOID WAS FALSIFIED, NOT ASSUMED. Memoising the compiled document by name — the exact defect the verification clause exists to catch, and the change that would report the best numbers this benchmark has ever produced — makes the harness report VOID with a non-zero exit and NO timing figures at all, because the compile counter stops advancing between measured requests. ⚠️ THE COMPILE COUNTER IS NOT GATED BEHIND AN ENVIRONMENT VARIABLE. An instrument that must be switched on can be left off, and a benchmark that verifies nothing is worse than none.",
    "observedAt": "2026-08-28",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Six valid runs of ACC-0020's protocol against the largest stage document (02-intent-decomposition.md, 21,047 bytes): production build, ten discarded warm-up requests, fifty serial measured requests, compile counter verified to advance 49/49 in every run. THREE RUNS PASSED AND THREE MISSED. Combined p95 by run: 253.72, 246.22, 314.65, 374.68, 342.18, 245.31 ms against a 300 ms budget; the upper-bound span (read start to complete markup received) tracked it at 267, 255, 327, 387, 357, 251 ms. THE SPLIT IS THE RESULT: MDX compilation was 15.41-29.05 ms p95 in every run, between 4% and 12% of the span, while the planning-content read around it was 233.87-358.89 ms p95. The variance that decides the verdict lives entirely in the read.",
    "title": "Benchmark: request-time compile costs 16-29 ms; the read around it costs 234-359 ms",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "contentRoot": "a disposable copy; the project's own content was not touched",
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start",
        "node": "v24.18.0",
        "os": "Windows 11",
        "write": "lib/tools/review-status.mjs, unmodified and reused"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "cross-process-concurrency",
          "reason": "The concurrent submissions were eight in ONE process. The lock is a file lock and is meant to hold against the CLI writing at the same time as the server, which was not exercised — a second process would have to be started and made to contend on purpose.",
          "state": "not-captured"
        },
        {
          "fact": "browser-submission",
          "reason": "The POST was issued by `fetch` with the action id scraped from the served HTML, which is the same request a browser makes with JavaScript disabled. Whether a real browser's form submission and its client-side navigation behave identically was not observed; no browser was available.",
          "state": "not-observable"
        },
        {
          "fact": "lock-contention-timing",
          "reason": "That the lock SERIALISES was established by an observable consequence — every submission saw the previous one's value — not by timing acquisition. How long a submission waits behind another was not measured.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0072",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE SAFETY ASSERTIONS WERE NOT ENOUGH, AND MEASURING SAID SO. An unlocked read-modify-write was substituted for the real one and passed every check that the file parses, keeps its fields, ends in a newline and holds one of the submitted values — because small writes do not visibly tear. Serialisation shows up somewhere else: under the lock every submission reads what the previous wrote, so eight alternating submissions all change something (8/8); without it they all read the same stale value and three report no change (6/8). Measured, stable over five runs of each, and now asserted. The weaker version of this test would have passed for the wrong reason. ⚠️ `lifecycle` IS UNREACHABLE BY CONSTRUCTION, NOT BY CARE. The adapter hands out exactly one operation and its signature has no field parameter, so there is no argument this path could pass and no string a form could post that would retire an artifact. `retired` and `lifecycle` are in the rejected-status list deliberately: they are what someone would try. ⚠️ NO `type` FIELD CROSSES THE WIRE. The form posts an id and the type is derived from it through the schema set, so a submission cannot name one artifact and have another written. ⚠️ FALSIFIED FOUR WAYS, each failing only its own check: adding `setLifecycle` to the review adapter; opening a second write door in the content adapter; making the action report success without calling the write (the end-to-end disk assertion fails, and nothing else does); and giving the permitted synchronous reader export a real read.",
    "observedAt": "2026-08-29",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Fourteen tests against the shipping write path with the REAL locking operation injected over a temporary content root, plus a genuine end-to-end POST to the Server Action in a production build. Measured: a successful submission changes `reviewStatus` on disk and leaves the artifact's `lifecycle`, its other fields and every other file byte-identical (verified by hashing the whole content root before and after — exactly one file differs); an invalid status, an unknown artifact, a malformed id and an approval with no reviewer are each refused with nothing written; concurrent conflicting submissions leave one valid artifact with no lost fields; and the redirect target is matched against a literal shape, so `//evil.example`, `javascript:` and `/stage/../../etc/passwd` all fall back to the site root. IN PRODUCTION, against a real build serving a temporary content copy, a form POST carrying the action id Next.js renders for no-JavaScript submission returned 303 to the same stage URL with the same artifact selected, the artifact on disk had changed, and a FRESH request rendered the new status; a refused submission came back as `?reviewError=bad-status`, rendered its sentence, left the artifact untouched and left the rest of the page intact.",
    "title": "Experiment: the review-status write, from a real form POST to the persisted artifact",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "command": "npm start (node bin/start-shell.mjs)",
        "framework": "Next.js 16.3.2",
        "mode": "next build + next start, spawned directly",
        "node": "v24.18.0",
        "os": "Windows 11",
        "port": "4413 in the test; 3000 by default"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "posix-signal-path",
          "reason": "The graceful stop was exercised through stdin, not through SIGTERM, because on Windows `process.kill` is `TerminateProcess` and no handler runs. The POSIX branch of the shutdown code (`child.kill('SIGTERM')` then `SIGKILL`) has never been executed — no Linux or macOS host was available.",
          "state": "not-captured"
        },
        {
          "fact": "clean-machine-install",
          "reason": "The install branch was not exercised: `node_modules` was present in every run, so the launcher took the `skipping install` path. ACC-0033's 'no prior application state' therefore rests on the build-and-start half, not on a genuinely cold install.",
          "state": "not-captured"
        },
        {
          "fact": "who-killed-the-child",
          "reason": "See the notes. On this platform the child dies when its parent exits regardless of whether the launcher terminated it, so the pid observation cannot attribute the kill. The launcher's own report of having SEEN its child exit is what distinguishes the two, and that is what is asserted.",
          "state": "not-observable"
        }
      ]
    },
    "id": "EVD-0073",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THE THREE CLEANUP OBSERVATIONS ARE NECESSARY AND NOT SUFFICIENT, AND THAT WAS MEASURED RATHER THAN REASONED. With child termination and its escalation commented out of the launcher entirely, the child was STILL gone 500ms after the launcher exited — Windows reaps it when its parent goes — so the first version of this test passed against a launcher that terminated nothing. Exactly the defect the review-write concurrency test had, found the same way. ⚠️ WHAT DISTINGUISHES THEM IS WHETHER THE LAUNCHER SAW ITS CHILD EXIT. Terminating it, the launcher observes the exit and reports `stopped.`; not terminating it, it waits out the grace period, escalates, and reports `stopped, but the child did not report exiting`. The message and the elapsed time are both asserted, and with termination disabled the test now fails on the message. ⚠️ A GRACEFUL SIGNAL IS NOT DELIVERABLE ON WINDOWS, WHICH THE FIRST RUN DEMONSTRATED. `process.kill(pid, 'SIGTERM')` is `TerminateProcess`: the launcher died instantly, no handler ran, cleanup never happened, and two run directories survived in the temp root. Ctrl+C reaches the process everywhere and is the operator's path; a supervising process writes `stop` to stdin or closes it. The affordance exists because the guarantee was otherwise unprovable on the platform this project runs on. ⚠️ THE HEARTBEAT PRECONDITION IS LOAD-BEARING. The watcher is created on first subscription, so a test that started the launcher and stopped it would observe the flawless teardown of something that never existed, and would keep passing if the watcher were never released.",
    "observedAt": "2026-08-29",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Three tests against the shipping launcher. Measured: `npm start` installs if needed, builds for production, starts, and the project view responds with its reading panel resolved and no further steps; the content root is printed and is absolute; a content root that does not exist is a refusal with exit code 2 naming the path and the variable; Next is spawned directly, so the recorded pid is the server's rather than an npm shim's. Then, after subscribing to `/events` and observing a NAMED heartbeat — proving a watcher existed, since the service creates it lazily on first subscription — the launcher was stopped and observed: the child pid is gone, the port binds again, its run directory is removed, and no `vpw-launch-*` directory survives anywhere in the temp root.",
    "title": "Experiment: the launcher's cleanup, and what a cleanup observation cannot prove on Windows",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "command": "npm start, in a copy holding only git-tracked files",
        "framework": "Next.js 16.3.2",
        "node": "v24.18.0",
        "os": "Windows 11",
        "port": "4415",
        "readyIn": "27.2s from nothing to serving",
        "stoppedIn": "413ms"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "cold-npm-cache",
          "reason": "The repository copy had no prior application state, which is what ACC-0033 asks. The MACHINE's npm cache was warm, which is why install took seconds rather than minutes. A host that had never fetched these packages would be slower; nothing here measures that, and no clause of the criterion depends on it.",
          "state": "not-captured"
        },
        {
          "fact": "different-machine",
          "reason": "'A machine with the repository and Node present' was approximated by a directory on this machine holding only tracked files. A genuinely different host — another OS, another Node minor, no global toolchain — was not available.",
          "state": "not-observable"
        },
        {
          "fact": "posix-shutdown",
          "reason": "As in EVD-0073: the stop went through stdin because Windows cannot deliver a graceful signal. The POSIX branch of the launcher's shutdown has still never executed.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0074",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THIS RUN EXISTS BECAUSE THE CRITERION WAS TICKED WITHOUT IT. ACC-0033 was marked `pass` on EVD-0073, whose own omissions said the install branch had never been taken — `node_modules` was present in every run, so the launcher printed `skipping install` each time. Half the criterion was measured and the whole of it was claimed. The omission was recorded accurately and then contradicted three lines later by the outcome, which is worse than not recording it: it produces a document that disproves its own conclusion to anyone who reads both halves. ⚠️ THE INSTALL BRANCH IS ASSERTED POSITIVELY, not inferred from success. The run checks for `installing dependencies…` AND for the absence of `skipping install`, because a launcher that silently skipped would still have built, served and cleaned up perfectly — and would have produced output indistinguishable from this one at every other point. ⚠️ THE CONTENT ROOT WAS CHECKED TO BE INSIDE THE COPY. A launcher resolving back to the source repository would serve the same pages from the same content and look entirely correct, while proving nothing about a fresh checkout.",
    "observedAt": "2026-08-29",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "`npm start` run in a disposable copy of the repository containing only the 512 git-tracked files — no `node_modules`, no `.next` — confirmed absent before starting. Measured: the launcher took the INSTALL branch (`installing dependencies…`, not `skipping install`), built for production, and was ready in 27.2s with no further steps. It printed a content root that is absolute and inside the cold copy rather than the source repository. The project view served its route marker with the reading panel and diagnostics resolved; `/events` produced a named heartbeat, so a watcher existed. Stopped via stdin: the launcher confirmed it saw its child exit in 413ms with no degraded message, the child pid was gone, the port bound again, its run directory was removed, and no `vpw-launch-*` directory was left anywhere in the temp root.",
    "title": "Experiment: the documented command in a cold copy — install, build, serve, clean up",
    "type": "evidence"
  },
  {
    "environment": {
      "execution": "host",
      "facts": {
        "browser": "Google Chrome, headless=new, driven over CDP with no automation library",
        "contentRoot": "a disposable copy; the project's own content was not touched",
        "framework": "Next.js 16.3.2",
        "harness": "bin/browser-check.mjs",
        "mode": "next build + next start",
        "node": "v24.18.0",
        "os": "Windows 11",
        "recoveredAfter": "783ms from the server returning",
        "staleAfter": "12.7s from the moment it was observed live",
        "watchdogWindow": "15s, the shipped default"
      },
      "isolationBoundary": {
        "doesNotClaim": [
          "containment-of-hostile-code",
          "host-filesystem-denial",
          "network-isolation",
          "process-isolation"
        ],
        "isolates": []
      },
      "omissions": [
        {
          "fact": "other-engines",
          "reason": "Chrome only. Firefox and WebKit have their own EventSource reconnection behaviour and their own brand checks; neither was available on this host. The defect found here is a WebIDL requirement all of them share, but that is an argument, not a measurement.",
          "state": "not-captured"
        },
        {
          "fact": "headed-browser",
          "reason": "Headless. `document.visibilityState` reported `visible` and timers were shown to run at full speed, so the two behaviours under test were not affected — but a headed window was never opened, and nothing here observes what a person would actually see.",
          "state": "not-captured"
        },
        {
          "fact": "long-disconnection",
          "reason": "The server was down for roughly three seconds. EventSource's own backoff after repeated failures, and what the page does across a much longer outage, were not exercised.",
          "state": "not-captured"
        }
      ]
    },
    "id": "EVD-0075",
    "kind": "experiment",
    "lifecycle": "active",
    "notes": "⚠️ THIRTEEN PASSING UNIT TESTS DESCRIBED A COMPONENT THAT DID NOT WORK AT ALL. Every one of them injected timers, so not one of them ever called the default — and the default was the broken part. The shipped page went live and stayed live forever: not a degraded indicator, but the exact failure the component exists to prevent, and the most reassuring possible form of it. This is what 'inject the collaborators' costs when nothing ever exercises the real ones. ⚠️ THE PM'S CONSTRAINT IS WHAT FOUND IT. 'Do not inject EventSource or fake timers for this evidence' was the whole reason the run could fail. Any harness that supplied its own timers — which is to say, any faster or more convenient harness — would have reported both criteria passing. ⚠️ THE STALE CASE WAS PROVOKED WITHOUT BREAKING ANYTHING. The heartbeat interval was pushed past the client's window while the connection stayed open, and CDP network events confirm the `/events` request received a response and never finished or failed. Had the socket dropped, the page would have gone stale down the ERROR path and the watchdog would never have been the thing under test. ⚠️ COLOUR IS NOT DOING THE WORK. The text changed from `Receiving updates` to `Not receiving updates` and the icon changed from a tick to a warning triangle. The colour changed too, and the assertion does not depend on it.",
    "observedAt": "2026-08-29",
    "outcome": "success",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "summary": "Headless Chrome driven over the DevTools protocol against the real production build, with no injected EventSource and no injected timers. THE FIRST RUN FAILED ACC-0029: with the server's heartbeat interval set to 600s and the socket confirmed still open, the page stayed `Receiving updates` for the whole 40s budget. Diagnosis, measured rather than assumed: `document.visibilityState` was `visible` and an in-page 15000ms `setTimeout` returned in 15014ms, so timer throttling was excluded; a raw-socket read of `/events` showed exactly one heartbeat at 32ms then silence with the socket open, so the server was correct. The cause was `timers = { setTimeout, clearTimeout }` throwing `Illegal invocation` inside the open handler (AST-0040). AFTER WRAPPING THE DEFAULTS, both criteria pass: the indicator went from live to `Not receiving updates` while the stream was still open, changing its TEXT and its ICON and its colour; and after the server was dropped, an attestation changed, and the SAME build restarted, the page reconnected on its own, reloaded, and showed the changed stage 783ms later with its URL preserved.",
    "title": "Experiment: ACC-0029 and ACC-0030 in a real browser — and the defect that only a browser could find",
    "type": "evidence"
  }
]
