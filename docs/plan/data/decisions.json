[
  {
    "addresses": [
      "QST-0004"
    ],
    "alternatives": [
      {
        "option": "Migrate the skeleton to Next.js now, as cleanup",
        "whyNot": "Framework conformity is not a benefit. It would replace a working proof of the substrate with an equivalent that costs effort and adds MDX, components and the site build that nobody needs yet."
      },
      {
        "option": "Commit to staying framework-free",
        "whyNot": "Forecloses MDX, the interactive components #54 names as the v1 flagship, and #19's site/ output — all real product requirements, none of which the skeleton was built to serve."
      },
      {
        "option": "Decide when the first UI feature is specified",
        "whyNot": "Chosen, in effect: this IS that decision, taken now so the skeleton's continued existence is deliberate rather than unexamined."
      }
    ],
    "decidedAt": "2026-08-18",
    "evidencedBy": [
      "AST-0006",
      "AST-0008",
      "AST-0009",
      "AST-0011",
      "AST-0012"
    ],
    "id": "DEC-0001",
    "lifecycle": "active",
    "notes": "AST-0010 — Cache Components under `next build` — becomes BLOCKING only when a production build enters scope. Until then testing it would validate a deployment mode the project has not decided to use, and #2 and #22's local-only stance mean it may never. The skeleton is retained deliberately as proof of the substrate, not as a deviation owing a migration.",
    "rationale": "The skeleton was framework-free because #5's choice was untested; QST-0004 has now tested it, including the surface the skeleton deliberately left open. Nothing forces a migration — the framework-free server already does what Next.js was validated to do. What Next.js buys is product: MDX rendering, the interactive components #54 wants, and #19's site/ build path. Migrating for its own sake would trade a working proof of the risky substrate for no capability.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "Do not migrate the walking skeleton to Next.js as cleanup. When product UI work begins, build the product shell in Next.js, carrying forward the validated watcher/SSE/write-back contract. Migration is feature work, not framework conformity.",
    "title": "Adopt Next.js when product UI work begins, not as cleanup",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0009"
    ],
    "alternatives": [
      {
        "option": "Everything unresolved blocks",
        "whyNot": "Nothing would ever exit a stage, and the criterion would stop carrying information — the same erosion #46 describes for a lint that blocks mid-thought."
      },
      {
        "option": "Only what stops work today blocks",
        "whyNot": "Too narrow. It permits making a load-bearing commitment while a question that decides its correctness is open, which is precisely the failure the gate exists to catch."
      },
      {
        "option": "Leave it to judgement each time",
        "whyNot": "The criterion would mean something different at every gate, and #93's attestation would record a verdict with no shared premise behind it."
      }
    ],
    "decidedAt": "2026-08-18",
    "id": "DEC-0002",
    "lifecycle": "active",
    "rationale": "Stage 4's exit criterion turns on the word and the stage table never defined it, so the stage could not be exited honestly in either direction. The definition separates the two things that were being conflated: a decision whose absence would make the next commitment possibly wrong, and work that is simply not done. The second clause is self-referential on purpose — a question that makes a criterion unevaluable blocks by construction, which is what QST-0009 itself was.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "An unresolved question is blocking when proceeding would require a load-bearing commitment whose correctness materially depends on its answer, or when its absence makes the current stage's exit criterion unevaluable. Missing implementation alone is work, not a gap.",
    "title": "What makes a requirement gap BLOCKING",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0005",
      "QST-0006"
    ],
    "alternatives": [
      {
        "option": "Let a specialist degrade gracefully when a capability is missing",
        "whyNot": "Graceful degradation is indistinguishable from the failure #67 found. The output looks the same and says nothing about what it could not do."
      },
      {
        "option": "Check capabilities once at startup rather than per delegation",
        "whyNot": "A credential can expire and a sandbox can go away mid-session; #108's stale-blocker lesson is that a capability recorded once is a claim with a shelf life."
      }
    ],
    "decidedAt": "2026-08-18",
    "evidencedBy": [
      "AST-0004"
    ],
    "id": "DEC-0003",
    "lifecycle": "active",
    "notes": "Generalises #81 from typed tools to any declared capability. #67(b) remains the unwritten half of both.",
    "rationale": "#67 measured the failure this prevents: a trust-denied child completed a normal-looking session with the typed tools absent and nothing reported it. #81 answered that for tools specifically, with a capability check before any output is accepted. This generalises it: research and sandboxing are capabilities in exactly the same sense, and a research specialist with no search, or a validation specialist with no sandbox, fails the same way — plausible prose in place of an answer. Detectability is the load-bearing half: a capability the host cannot check is one the contract cannot honestly promise.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "Every capability named in a specialist contract must be one the host can actually provide and can verify is present. Where a named capability is unavailable, the specialist must produce an explicit refusal or a recorded gap — never proceed by guessing.",
    "title": "A specialist contract may only describe capabilities the host can supply AND detect",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0005"
    ],
    "alternatives": [
      {
        "option": "Let the specialist call an MCP server's tools directly",
        "whyNot": "The contract would name tools the host does not own. Renaming or replacing the backend would silently change what the specialist can promise, and DEC-0003's detectability requirement would have nothing stable to probe."
      },
      {
        "option": "Allow authenticated and private sources from the start",
        "whyNot": "Side effects and credentials are a different risk class from public reads. Deferring them keeps the first contract honest and leaves activation as a deliberate, separate act (#77's shape)."
      },
      {
        "option": "Let the specialist fall back to model memory when retrieval fails",
        "whyNot": "That is the exact failure the project exists to fix, and it is indistinguishable from success in the output. A structured refusal is a worse answer and a better record."
      }
    ],
    "assumesThat": [],
    "decidedAt": "2026-08-22",
    "id": "DEC-0004",
    "lifecycle": "active",
    "notes": "Sources for the Pi facts: packages/coding-agent/docs/usage.md and docs/extensions.md (badlogic/pi-mono). ⚠️ Those are documentation reads, not experiments — if a claim about Pi's extension API becomes load-bearing it needs evidence(kind: source) with retrieval time, per this decision's own rule. The first adapter is implementation work, not a blocking design question, BECAUSE changing it cannot alter the specialist contract — that is what makes the backend replaceable rather than merely intended to be.",
    "rationale": "This answers QST-0005 at the level DEC-0003 requires: a capability the host can supply AND detect. Naming three typed tools rather than a backend makes the contract falsifiable — the specialist can be asked whether it has research, and the answer is a probe result rather than an assumption. Pi has no built-in MCP and its extension API registers custom tools, so the adapter layer is where a vendor belongs; a contract written against vendor tool names would change every time the backend did, which is #67's toolless child arriving through the supply chain instead of through trust. The refusal clause is the load-bearing half: the intake's problem 2 was 'educated guessing', and a research specialist that answers from model memory when retrieval is down reproduces exactly that failure while looking like success. Discovery-is-not-evidence keeps #123's boundary intact at the point new observations enter — a search hit is an observation about what exists, not an observation about the claim.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "The shipped Pi extension owns a stable, typed research interface — `research_search`, `research_fetch`, `research_capability` — and the research specialist depends on THOSE capabilities, never on a search vendor, model provider or arbitrary MCP tool name. An MCP server may sit behind the extension as an adapter; it must not become the specialist contract. Boundary: public, read-only internet research is permitted when a delegated question depends on a current external fact. Authenticated or private sources, purchases, submissions and other side effects are unavailable unless separately activated later. Credentials live in the PM's host environment, outside the repository and the planning content; the extension consumes them and they are never placed in a child prompt or returned in tool output. Every delegation probes tool registration AND backend health/authentication — installed-but-unusable is unavailable. Search results are discovery observations, not automatically evidence: a fetched source supporting a recorded claim must become evidence(kind: source) carrying URL, retrieval time, source metadata and a retained citation. If search or retrieval is unavailable the specialist returns a structured capability refusal and records the gap, and MUST NOT answer from model memory.",
    "title": "Research capability is a typed extension interface, not a vendor",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0006"
    ],
    "alternatives": [
      {
        "option": "Start at tier 2 (containers) for real isolation",
        "whyNot": "#77 makes tier 2 conditional on Docker already being present and otherwise a recommendation for the PM to approve. Building it first would make the default path depend on something not guaranteed, and tier 1's honest boundary is sufficient for trusted, non-destructive validation, which is what the current claims need."
      },
      {
        "option": "Give the validation specialist a shell and a working directory",
        "whyNot": "A shell is not a declarable job. Nothing could be refused before provisioning, no capture plan could be enforced, and the observation record would be whatever the specialist chose to report — which is the record deciding its own completeness."
      },
      {
        "option": "Call the venv a sandbox and move on",
        "whyNot": "It would silently inflate the confidence rung of every claim validated in it. The cost of the honest boundary is a smaller set of claims tier 1 may settle; the cost of the dishonest one is not knowing which claims those are."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0005",
    "lifecycle": "active",
    "notes": "⚠️ The omission states are now DECIDED as a contract, but the `evidence` schema field that carries them is still unbuilt — #122's deferred consequence lands here. Implementing the extension, the first research adapter and the tier-1 controller is work, not a stage 4 gap (DEC-0002).",
    "rationale": "This answers QST-0006 by settling boundaries before mechanism, as DEC-0003 requires. Four things make it a contract rather than an intention. (1) The honest tier-1 boundary: a venv called a sandbox would let a claim inherit containment it never had, and every claim validated in tier 1 would carry a strength nothing earned. Naming what tier 1 does NOT do is what keeps the rung ceiling truthful. (2) Argument arrays rather than shell strings remove a whole class of interpolation defect at the type level rather than by review. (3) The four omission states are exactly what #122 said the schema needed and #123 said it must not exceed: each is a fact about the observer, none is a judgement about a claim, and each is stated only because a capture plan can actually distinguish it — a state nothing can detect would repeat #121's error one layer down. (4) Cleanup failure as a first-class result closes the gap where a destroy step that silently failed would leave the record saying the environment was gone. Refusal before provisioning is what makes #77's authorisation real: a ceiling checked after spending is a report, not a control.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "Build tier 1 first, since #77 already makes it the default. The shipped extension provides a typed validation controller; the specialist NEVER receives unrestricted shell execution. A validation job declares: required tier and capabilities; input files or hashes; commands as an ARGUMENT ARRAY, never an interpolated shell string; a capture plan; timeout and output limits; expected outputs; and any network, credential or cost requirement. The controller owns provision -> execute -> observe -> destroy and refuses a job BEFORE provisioning if it exceeds the project's approved ceiling. Tier 1 is a fresh temporary workspace and Python virtual environment. Its boundary is stated, not implied: a virtual environment isolates Python dependencies; it is NOT an OS security sandbox. Tier 1 therefore does not claim to contain hostile code, deny host-filesystem access or enforce network isolation; it supplies no credentials and uses an allowlisted process environment, and is suitable only for trusted, non-destructive validation that does not need those guarantees. Claims requiring real containment, OS/package fidelity, credentials or external infrastructure require a higher tier. Every run produces an observation record: commands, inputs, outputs, exit status, timing, versions, environment facts, permission failures and cleanup outcome. The capture plan makes omission states DETECTABLE — `not captured` (collection deliberately disabled), `not observable` (no available collector), `unavailable` (collection ran, fact absent), `redacted` (obtained, policy suppressed) — each requiring a reason. Unrequested facts remain simply absent. These describe the OBSERVER only; bearing, applicability, verdict and confidence stay claim-relative (#123). Cleanup failure is a first-class run result: preserve the evidence, report the retained path, and never present destruction as successful.",
    "title": "Tier 1 first, through a typed validation controller with an honest boundary",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0011"
    ],
    "alternatives": [
      {
        "option": "A billing-backed free allowance (major cloud search APIs)",
        "whyNot": "A free tier that keeps serving past the allowance and bills for it puts REQ-0012's authorisation ceiling outside the system's control. Stopping at exhaustion is the property that was wanted."
      },
      {
        "option": "Probe by performing a cheap search",
        "whyNot": "It spends a credit to learn the capability exists, and it cannot distinguish 'auth is fine but quota is gone' from 'auth failed' as cleanly as the usage endpoint does."
      },
      {
        "option": "Let the caller choose search depth and result richness",
        "whyNot": "Those parameters are what the query costs. A caller that can set them can spend more than the PM authorised, which converts a control into a suggestion."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0006",
    "lifecycle": "active",
    "notes": "⚠️ RESIDUAL RISK, stated rather than closed: DNS rebinding. The guard resolves and checks every address, then fetch resolves the name again itself, so a hostile authority answering differently the second time can still move the destination. Closing it needs a connection to the checked IP with an explicit Host header and SNI, which Node's fetch does not expose. The honest position is that the boundary raises the cost of an attack rather than eliminating it — the same kind of statement DEC-0005 makes about tier 1. ⚠️ The Tavily facts here (free-plan behaviour, the usage endpoint, per-search cost) are DOCUMENTATION READS supplied by the PM, not experiments. Under DEC-0004's own rule they become evidence(kind: source) only when fetched and recorded with retrieval time — which is exactly what 7a step 4 will do, using this adapter, once the credential exists. ⚠️ MEASURED 2026-08-22, and it differs from the documentation read this decision cites: the live probe reported a plan limit of 1500 credits, not 1,000. Recorded rather than reconciled — the difference could be a changed free tier, a different plan, or a promotion, and this run cannot distinguish them. It does not disturb the decision, because the property that mattered was 'no payment card, stops when exhausted', and NEITHER figure is what carried it. ⚠️ The stopping behaviour itself remains UNTESTED under either reading.",
    "rationale": "Tavily fits REQ-0012 better than the alternatives for a specific reason: its free plan grants 1,000 monthly credits with no payment card and STOPS when exhausted, so the authorisation ceiling is enforced by the plan rather than by the PM watching a bill. GET /usage is what makes the capability probe meaningful — DEC-0003 requires a capability the host can DETECT, and a backend that could only be tested by performing a search would charge a credit to answer 'are you there'. Fixing the spend-relevant search parameters in the adapter rather than exposing them keeps cost predictable at one credit per search, which is a REQ-0012 control and not a default. The public-web boundary is the sharper half: a fetch tool with no destination boundary is not a research capability but a request forger inside the host — 169.254.169.254 is cloud metadata and localhost is whatever the PM is running — so DEC-0004's 'public, read-only' scope needed a mechanism rather than a sentence. Revalidating every redirect is what makes the boundary hold, since checking only the URL the caller supplied inspects the doormat and lets the server choose the destination.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "The first adapter behind DEC-0004's typed interface is the Tavily Search API. Basic search is configured with search_depth 'basic', auto_parameters false, include_answer false and include_raw_content false, and returns discovery results only. GET /usage is the capability probe: it validates authentication and reports remaining quota WITHOUT performing a search. CREDENTIAL CONTRACT: the PM creates the account and supplies TAVILY_API_KEY through the host environment; it is injected only into the research child, never into planning or validation children; the specialist has no shell and no generic environment-reading tool; the extension reads the key but never places it in prompts, logs, errors or tool results; and missing key, failed authentication, exhausted quota and an unreachable backend each produce a DISTINCT unavailable reason. PUBLIC-WEB BOUNDARY on research_fetch: reject non-HTTP(S) schemes, URLs carrying credentials, loopback/private/link-local destinations, and oversized or unsupported responses, and revalidate EVERY redirect. Tavily discovers candidate URLs; the project's own guarded fetch reads the authoritative page; neither becomes evidence until the specialist records the fetched source through the typed evidence path.",
    "title": "Tavily is the first research adapter, with a stated credential and public-web boundary",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0013"
    ],
    "alternatives": [
      {
        "option": "Implement the tier→rung ceiling as written",
        "whyNot": "It conflates provisioning with epistemic strength. A host run would be capped for a claim about that host, and a tier-1 run would be credited for containment it states it does not provide."
      },
      {
        "option": "Invent a rank ordering host < tier 1 < tier 2 < tier 3",
        "whyNot": "A rank implies more isolation is always better evidence, which is false: for a claim about the host's own behaviour, the host IS the matching environment and a container is a mismatch."
      },
      {
        "option": "Leave confidence untouched by isolation entirely",
        "whyNot": "Then a container run could carry a kernel-module claim to the top rung — the NVIDIA-driver failure the rung table was written to prevent."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0007",
    "lifecycle": "active",
    "notes": "⚠️ Amends prose in two places rather than only adding code: #77's third consequence and the 'Which tier can reach which rung' section both stated the ceiling as the mechanism. The table survives as a summary of what usually follows — the isolation an OS-packages claim needs is what tier 1 says it does not supply — but it is a consequence, not a lookup the code performs. ⚠️ Consequence for the tier-2 recommendation's wording: not 'without Docker nothing exceeds rung 2', but 'without Docker, these specific claims cannot be settled here'.",
    "rationale": "QST-0013 looked like a missing implementation and was actually a category error preserved in the wording. Building the cap would have made two axes into one and produced wrong answers in both directions: it would have penalised AST-0013 — a claim about the PM's host, evidenced by a run on that host, where the absence of isolation is not a shortfall but an irrelevance — and it would have rewarded a tier-1 run for a containment claim tier 1 explicitly does not support. Routing isolation through the existing three-state comparison keeps one mechanism where there might have been two, and it makes DEC-0005's `doesNotClaim` list load-bearing: an explicit denial is a CONTRADICTION of a requirement, where silence is only unknown. The neutral 'not-required' state is the part most easily got wrong — collapsing it into 'unknown' would reintroduce the global penalty in different words, by capping every claim that never asked about isolation.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "There is NO global isolation-to-confidence cap. Sandbox tier describes how execution was provisioned and what boundary it supplied; confidence describes how well applicable evidence matches a particular claim. A host run may legitimately reach environment-matched for a claim specifically about that host, and tier 1 cannot lift an OS-containment claim merely because a virtual environment existed. `isolationBoundary` therefore enters the same claim-relative comparison as every other environment fact, via the assertion's `requiresIsolation`: required and matched may reach environment-matched; required but absent is unknown and capped; required and incompatible means the evidence does not apply to that claim; not required by the claim carries no penalty at all. The older 'tier determines the confidence ceiling' language is AMENDED rather than implemented.",
    "title": "Tier is a permission and execution boundary, not an epistemic rank",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0014",
      "QST-0010"
    ],
    "alternatives": [
      {
        "option": "Record the requirement-to-component edge as a string on the requirement",
        "whyNot": "#85's prose assumption in a different costume. A string cannot be traced, counted or flagged, and the stage-9 orphan check needs traversal."
      },
      {
        "option": "Leave the trace as prose in an architecture document",
        "whyNot": "Section 1 of the stage 5 document IS that prose, and no gate can read it. The criterion asks for something a reader can check mechanically."
      },
      {
        "option": "Drop schema, api-spec and wireframe from stage 5 since this run needed none",
        "whyNot": "It confuses inapplicable-here with invalid-globally. A consumer project designing a real service would need all three, and produces[] is the envelope across project types."
      },
      {
        "option": "Add richer links now (acceptance criteria, tasks, dependencies)",
        "whyNot": "No consumer has demanded them. A field added for an acceptance criterion nobody has written is the catalogue justifying itself again (#106)."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0008",
    "lifecycle": "active",
    "notes": "⚠️ Approving this made QST-0010 an IMMEDIATE prerequisite rather than deferred work: activating by hand once more would have been the fifth instance of #126's pattern, committed knowingly. `lib/tools/activate-type.mjs` now performs the transition — validating catalogue membership, authorability (schema AND typed tool) and reachability (some stage must produce it) before writing only `capabilities.artifactTypes.activated`. It never edits stages/, schemas/ or the catalogue (#95, #121's correction). Deactivation refuses while live artifacts of the type exist, so #107's silence cannot start with a manifest edit.",
    "rationale": "The #41 case is conclusive rather than speculative: `component` owns irreducible many-to-many design state that a requirement cannot hold, and four independent consumers were already decided before the type existed — #60's cascade, #19's role slicing, the stage-9 orphan check, and stage 5's own third exit criterion. The gap was old: notes.md names 'design component' three times in the traceability chain and #38's sixteen omitted it, so a chain four decisions rest on had a missing link that nothing noticed until a stage was actually run. The produces[] reframing preserves the useful draft table while correcting its overstatement: two stage runs found declared types legitimately unused, and the honest reading is that the table describes a project type rather than a stage. The gate already computed the intersection, so this is wording catching up with behaviour rather than a change of behaviour.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "`component` joins the v1 catalogue with prefix CMP and is approved for activation here. Minimal shape: a named design unit (the envelope's title), a `responsibility`, `satisfies` links to requirements, and the standard lifecycle/review envelope. Additional links wait for authentic stage 7-8 demand. Separately: a stage's `produces[]` is a CAPABILITY ENVELOPE across project types, and the applicable set for a particular project is `produces[] INTERSECT activatedTypes`. Activation describes the project type; the stage definition owns where an applicable type is produced. `component` is added to stage 5's produces; `schema`, `api-spec` and `wireframe` are RETAINED, because the stage 5 run showed them inapplicable here rather than invalid globally.",
    "title": "`component` joins the catalogue as CMP and is activated on this project",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0007"
    ],
    "alternatives": [
      {
        "option": "Generate the package as part of the stage-9 gate",
        "whyNot": "The gate would stop being answerable: running it to ask 'are we ready' would rewrite the repository. A read-only check and a writing generator are different tools with different safety properties."
      },
      {
        "option": "Let the planning agent assemble the handoff freeform",
        "whyNot": "Two runs would differ, and a package that cannot be reproduced cannot be reviewed or diffed. It also reintroduces the model as the source of truth for content the artifacts already hold."
      },
      {
        "option": "Defer the export to v2 and hand over planning-content directly",
        "whyNot": "REQ-0010 requires a recipient to begin without asking the planner, and raw planning-content is a store, not a package: no role slice, no ordering, and no rendering for the human half of REQ-0011."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0009",
    "lifecycle": "active",
    "notes": "⚠️ This decides that the export EXISTS and how it is invoked. It does not yet decide the package's contents beyond what DEC-0011 settles about surfaces, nor the role-slicing algorithm (#19), which is stage 8 work.",
    "rationale": "Separating the gate from the generator keeps two different failure modes apart. A gate that generated as a side effect could not be run to ASK a question — every check would mutate the repository, and #46's 'warn continuously' becomes 'rewrite continuously'. A freeform agent task fails the other way: the package would differ between runs, and a handoff that cannot be reproduced cannot be reviewed. An explicit command is reproducible, inspectable in a diff, and callable by a human or by CI without either of them approving anything. The folder in the repo IS the delivery (docs/plan/README.md), so there is no service to keep alive and the artifact of the handoff is a commit.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "A v1 export exists. It is produced by an explicit deterministic command — `npm run handoff` — that reads planning-content and writes the frozen package under docs/plan/. Generation is NEVER a gate side effect and never a freeform agent task: the gate stays READ-ONLY and reports whether the package may be produced, and the command is what produces it. `CMP-0011` is the component that will implement it, and it satisfies REQ-0010 and REQ-0011.",
    "title": "The v1 handoff export exists, and is produced by an explicit deterministic command",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0008"
    ],
    "alternatives": [
      {
        "option": "Ship the skeleton as the delivery surface",
        "whyNot": "It renders CURRENT state, and a handoff must be frozen. It also requires the recipient to run a server to read a plan, which contradicts the zero-infrastructure delivery."
      },
      {
        "option": "Markdown only, no canonical JSON",
        "whyNot": "The automated half of REQ-0011 would be served by parsing prose, which is what the whole artifact model exists to avoid."
      },
      {
        "option": "Build an interactive recipient site now",
        "whyNot": "No demand has been demonstrated. QST-0008 asked whether the skeleton suffices; the answer is that the QUESTION was mis-framed - the delivery surface is a different artifact from the authoring surface, and neither needs to be an app for v1."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0010",
    "lifecycle": "active",
    "notes": "⚠️ Reframes rather than answers QST-0008 as asked. The question was 'is the skeleton's rendering sufficient for the human half', and the answer is that the skeleton is the wrong candidate — sufficiency should be judged of the GENERATED package, not of the authoring tool.",
    "rationale": "REQ-0011 requires that both a human reader and an automated consumer can recover every required item. It does not require that they use the same surface, and reading it as 'therefore build an app for the recipient' inflates a requirement about legibility into a commitment about product. Generated Markdown satisfies the human half with no infrastructure — it renders in any editor, in the repository host, and in a diff — while the JSON the artifacts already are satisfies the agent half without a second serialisation to keep in sync. Keeping the authoring surface out of the delivery also preserves the property that the handoff is a frozen package: a live app shows current state, and a handoff must show the state that was approved.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "For v1: canonical JSON serves automated consumers, and generated Markdown — including a top-level PLAN.md — serves human readers. The authoring skeleton (app/server.mjs) is a tool for working ON the plan and is NOT the delivery surface. An interactive site for the recipient requires separate demonstrated demand; it is not assumed by REQ-0011.",
    "title": "The authoring skeleton is not the delivery surface; v1 delivers JSON and generated Markdown",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0003"
    ],
    "alternatives": [
      {
        "option": "Build `runbook` now so the aggregate exists when it is needed",
        "whyNot": "It would be built on an imagined use. #106 exists for exactly this, and a type built early hardens conventions around a shape nobody has exercised."
      },
      {
        "option": "Remove `runbook` from the catalogue entirely",
        "whyNot": "That is #107's three-authority update - catalogue, stages/, and any project activation - and it is the PM's act, not a consequence of one project's handoff being thin. This project is a planning system; a deployment-shaped project would plausibly need the aggregate."
      },
      {
        "option": "Move the three fields onto `runbook-step`",
        "whyNot": "They are SEQUENCE-level by definition. A whole-run precondition on every step is the same fact repeated, and the first divergence between copies is a contradiction with no owner."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0011",
    "lifecycle": "active",
    "notes": "⚠️ REOPENING CONDITION, because this is a deferral and not a refutation: build `runbook` when a project's handoff contains a MULTI-STEP procedure with at least one destructive step and a rollback that spans steps — that is when cross-step state stops being imaginable and starts being owed. ⚠️ HONEST LIMIT ON THE EVIDENCE: this is one project's handoff, and this project is a planning system rather than a deployment. The same caution as #139's `produces[]` finding applies — it shows the type is inapplicable HERE, not that it is wrong. That is precisely why catalogue removal is not part of this decision. ⚠️ Deactivating it also makes `runbook-step.partOf` point at an unactivated type, which #75 already permits and declares; the edge dangles VISIBLY rather than being removed, so nothing is lost if the type is later built.",
    "rationale": "Applied to the real handoff rather than to the idea of a runbook. (1) WHOLE-RUN PRECONDITIONS: this project's package is generated by `npm run handoff` and read; it has no preconditions to satisfy before executing anything, and where a real deployment would have them, an ordered first step expresses them without an aggregate. (2) ABORT CRITERIA: `runbook-step` already carries `remediation`, `destructive` and `dependsOn`, and #58's floor is per-step by decision — an abort spanning steps is meaningful only for a multi-step destructive procedure, and v1's handoff contains one step that is `partOf` nothing. (3) CROSS-STEP ROLLBACK: the same, and it is the strongest of the three — a rollback spanning steps genuinely has nowhere to live. But nothing in this handoff needs one, and #106 is explicit that a type is built when work demands it, not when a plausible use can be imagined. Membership is derived by reversing `partOf` (#84), and ordering is `ordinal` plus `dependsOn`, so an aggregate is a PROJECTION at handoff rather than stored state.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "`runbook` is NOT built and is deactivated on this project. The three fields that would have justified it — whole-run preconditions, abort criteria, and cross-step rollback — are not needed by the handoff v1 actually produces. Membership in #38's catalogue is a separate act under #107's three-authority rule and is NOT performed here; the type remains in the catalogue, unimplemented and unactivated, with the condition that would reopen it recorded. `runbook-step.partOf` keeps its declared unresolvable target (#75), which is the mechanism that lets the edge exist while the aggregate does not.",
    "title": "`runbook` is not retained for v1: the real handoff needs none of its candidate fields",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0015"
    ],
    "alternatives": [
      {
        "option": "Add `state: planned | built` to component",
        "whyNot": "Stored derived state. Someone sets `built`, the code is deleted, and the field still says built. It also says nothing about whether the thing works, which is the question that actually matters."
      },
      {
        "option": "Infer built from a non-empty implementedBy alone",
        "whyNot": "That is the right signal for 'code exists' and the wrong one for 'accepted'. Collapsing them would let an unevaluated component read as finished."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0012",
    "lifecycle": "active",
    "notes": "⚠️ The three owners are now all real: `implementedBy` on component, `outcome` on acceptance-criterion (#144), and the stage attestation. Before #144 only two thirds existed, which is why the question was worth keeping open rather than closing on the argument alone.",
    "rationale": "QST-0015 was raised when CMP-0011 took the orphan count to zero while nothing was built, and running stage 7 dissolved it rather than answering it: the single question was three questions. The decisive objection to a field is #96's, applied in a new place - `built` is DERIVABLE from implementedBy plus criteria outcomes, and a derived value that gets stored is a value that can disagree with reality without anything noticing. The hazard the question named is real and is answered by making the derivation available rather than by adding a flag: ACC-0010 was written BEFORE the code and sat at `not-evaluated`, which is exactly the state a planned-versus-built boolean was wanted for.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "No status axis is added to `component`. Planned, implemented and accepted are three separate facts and each already has an owner: `implementedBy` says whether code exists, an acceptance criterion's `outcome` says whether it works, and the stage attestation says whether the plan claims it is finished. A `state: planned | built` field would answer none of them well and would be stored derived state, which can lie the moment the code it describes is deleted.",
    "title": "Planned, implemented and accepted are separate facts with existing owners",
    "type": "decision"
  },
  {
    "addresses": [],
    "alternatives": [
      {
        "option": "Activate `risk` so stage 6's criterion is mechanical",
        "whyNot": "It would make the criterion evaluable by satisfying its own premise. The prose register works, and the cost - a human reads a table - is stated in the attestation rather than hidden."
      },
      {
        "option": "Remove `risk` from the catalogue",
        "whyNot": "#107's three-authority act, and one project's absence of demand is not evidence about the type. The same restraint was applied to `runbook` and `research-finding`."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0013",
    "lifecycle": "active",
    "notes": "⚠️ Reopening condition: build `risk` when something OTHER than a stage-6 criterion must traverse one - a handoff carrying a risk register to a recipient, a cascade flagging risks when an assumption moves, or a requirement asking for risk tracking.",
    "rationale": "Stage 6 was run and produced exactly one consumer for `risk`: its own `high-severity-risks-mitigated` criterion. Compare `component`, whose four consumers were all decided before the type was proposed, and `acceptance-criterion` and `task`, which had the same property. The distinction is not how MANY consumers but whether any exists independently of the demand: a criterion saying 'every risk must be mitigated' cannot be evidence that risks need to be artifacts, because it presupposes it. Keeping the type in the catalogue rather than deleting it preserves the possibility that a deployment-shaped project has real downstream use, which this planning-system project does not.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "`risk` stays INACTIVE on this project and REMAINS in the catalogue. The general rule: when the only consumer that would traverse a type is the stage criterion that declares the type, that is circular demand and does not satisfy #41. A type is retained in the catalogue - not removed - until a project produces authentic downstream use.",
    "title": "A stage criterion demanding a type is circular demand, not an independent consumer",
    "type": "decision"
  },
  {
    "addresses": [],
    "alternatives": [
      {
        "option": "Leave `critical` undefined and judge case by case",
        "whyNot": "An exit criterion turning on an undefined word is unevaluable in either direction, which is what took DEC-0002 to fix once already."
      },
      {
        "option": "Define a severity scale for questions",
        "whyNot": "A second subjective category. DEC-0002's test is about consequence rather than felt importance, which is what makes it checkable."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0014",
    "lifecycle": "active",
    "notes": "⚠️ Applied before it was written: QST-0002 and QST-0015 were judged non-critical during the stage-9 run, by analogy to DEC-0002. That order is backwards and is recorded rather than tidied - the judgement stands, and the definition is what makes it reproducible by someone else.",
    "rationale": "Stage 9's run found `critical` undefined - the second instance of the defect stage 4 hit with `blocking`. Inventing a fresh category would double the vocabulary and leave two words that both mean 'important' with different unstated tests. DEC-0002 already separates a decision whose absence would make a commitment possibly wrong from work that is simply not done, and the handoff needs exactly that distinction scoped to what it publishes: a question is critical if the package would carry something wrong without its answer, or if a criterion cannot be judged.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "An unanswered question is CRITICAL for stage 9's `no-unresolved-critical-questions` when it blocks active handoff content or makes a handoff criterion unevaluable. This reuses DEC-0002's shape rather than inventing a second subjective scale, and the stage definition's criterion text is amended to say so.",
    "title": "Stage 9's `critical` is DEC-0002's definition, not a new category",
    "type": "decision"
  },
  {
    "addresses": [],
    "alternatives": [
      {
        "option": "Define stage attestations as sufficient package approval",
        "whyNot": "A recipient could be handed an instruction that no one reviewed, and the attestation would not have been about that instruction. Stage criteria are about the stage, not about each artifact in it."
      },
      {
        "option": "Require `approved` on every artifact before publishing",
        "whyNot": "It would demand sign-off on exploratory questions and on assertions kept for history, turning approval into a formality - the acknowledgement fatigue the risk register already names."
      },
      {
        "option": "Leave it ambiguous and let consumers infer",
        "whyNot": "That is the state the first package shipped in, and it is what produced this decision."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0015",
    "lifecycle": "active",
    "notes": "⚠️ CONSEQUENCE, and it will bite immediately: RBS-0001, TSK-0001 and TSK-0002 are all `draft`, so the handoff will REFUSE until they are reviewed. That is the decision working rather than an obstacle - the previous package shipped an instruction and two work items that nobody had signed. ⚠️ Approving requires a typed operation: `reviseArtifact` refuses `reviewStatus` as identity state, and the only writer today is the skeleton's write-back in app/server.mjs. That is the eighth instance of the missing-operation pattern and is being built with this decision rather than after it.",
    "rationale": "The first real package exported 98 artifacts as `draft` while describing itself as approved state, and the ambiguity was visible to machine consumers - which is the strongest kind of defect report, because the package said two things at once. Collapsing the two approvals either way would lose something real: making attestations sufficient would let a recipient act on an instruction nobody reviewed, and requiring per-artifact approval for everything would demand review of exploratory questions and superseded assertions that nobody needs to sign. The line is drawn at EXECUTABILITY because that is where the cost of being wrong changes: a draft question is a note, and a draft runbook step is an instruction someone follows. #57 and #59 already draw the same line for confidence and acknowledgement.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "The two are different approvals of different things and neither substitutes for the other. Stage attestations approve the PACKAGE: a human looked at each exit criterion and said satisfied, not-satisfied or n/a (#93). `reviewStatus` approves the ARTIFACT: someone reviewed that particular statement. For v1: (a) the package's MANIFEST and README state explicitly that the package is approved at STAGE level and carry the per-artifact review counts, so a machine consumer can see the basis rather than infer it; (b) artifact classes that become EXECUTABLE handoff content — `runbook-step` and `task` — must be `approved` or `amended` before they may be published, because those are the artifacts a recipient acts on; (c) every other class ships at whatever review status it holds, visible in the data.",
    "title": "Stage attestations approve the PACKAGE; reviewStatus approves the ARTIFACT, and executable content needs both",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0016"
    ],
    "alternatives": [
      {
        "option": "Keep MANIFEST and README outside the hash",
        "whyNot": "It excludes material files from the identity of the package that contains them, which is how two different packages came to share one snapshot."
      },
      {
        "option": "Add a generator fingerprint (hash of the renderer source)",
        "whyNot": "Optional rather than required once the output itself is fully hashed: a renderer change that does not alter any output should NOT change the package's identity, and a fingerprint would say it did."
      },
      {
        "option": "Require version discipline on package.json",
        "whyNot": "It makes identity depend on remembering to bump a number, which is the class of guarantee that fails silently. Worth doing for provenance; not sound as the identity mechanism."
      }
    ],
    "decidedAt": "2026-08-22",
    "id": "DEC-0016",
    "lifecycle": "active",
    "notes": "⚠️ Verification MUST normalize before recomputing - hashing a published package as-is hashes bytes the renderer never hashed and can never agree. `verifySnapshot` does it, and a test asserts that the UNNORMALIZED recompute does not accidentally match.",
    "rationale": "The previous scheme hashed every file EXCEPT the two that embed the snapshot, which excluded material content to avoid a self-reference. Republishing exposed the cost: adding the `approval` block to MANIFEST.json produced a materially different package under the same identity, b218b4a525c6176b. Normalizing removes the self-reference without excluding anything - a renderer change that alters any output now changes the snapshot, and one producing identical output correctly retains it, which is exactly the property that makes a package reviewable in a diff. It also relieves `toolVersion` of a job it was silently failing at: this repo's version is 0.0.0 and has never been bumped.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "Every file is rendered with a fixed placeholder where the snapshot goes; the entire tree - including MANIFEST.json and README.md - is hashed over canonical path names plus bytes; the placeholders are then replaced with the resulting hash. Verification performs the same normalization before recomputing. `toolVersion` is provenance metadata and carries no part of package identity. A separate generator fingerprint is optional and is not required.",
    "title": "The package snapshot is a normalized whole-package hash",
    "type": "decision"
  },
  {
    "alternatives": [
      {
        "option": "Migrate app/server.mjs into the Next.js application and delete it",
        "whyNot": "It removes the only working reference at exactly the moment a second implementation appears, so the first divergence would have nothing to be measured against. The skeleton's value is that it is verified; a migration converts it into unverified application code."
      },
      {
        "option": "Reimplement the lint, derivation and locking inside the Next.js app",
        "whyNot": "#47 has one implementation and three callers by design. A second copy inside the app becomes a second enforcement model within a month — the failure that row exists to prevent — and the lock in particular cannot be duplicated safely: two implementations of #78 are two lock protocols."
      },
      {
        "option": "Defer the shell and extend app/server.mjs instead",
        "whyNot": "#5 names Next.js and MDX and is untested by the skeleton (#100). Extending the skeleton indefinitely would keep deferring the only part of #5 that has never been exercised, while accumulating UI in a substrate that was explicitly not built to carry it."
      }
    ],
    "decidedAt": "2026-08-24",
    "id": "DEC-0017",
    "lifecycle": "active",
    "notes": "⚠️ Parity is the retirement condition, and it is deliberately not a date. `app/server.mjs` is retired when the Next.js slice demonstrably does what the skeleton's seven proven properties say it does — reads canonical files, derives at render time, persists nothing derived, refreshes on external change, writes through the lock, and surfaces lint findings without re-judging them. Until then two callers exist on purpose.",
    "rationale": "The skeleton exists to prove the risky surface — the watcher, the derivation and the status write-back — and it has a test suite asserting exactly those properties. Migrating it would spend that proof on a rendering framework it was deliberately built to avoid, and would leave nothing to compare the new shell against. Keeping it as a reference makes parity CHECKABLE rather than asserted: two callers of the same modules should agree, and a disagreement is a finding about the new shell.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "The Next.js/MDX application is built as a NEW shell that imports the existing lib/ modules — lint, derivation, locking and the typed operations — as the single implementation. `app/server.mjs` remains in place as the verified substrate reference until the Next.js slice reaches behavioural parity with it, and is not migrated, ported or edited to become the application.",
    "title": "The Next.js shell reuses the substrate; the skeleton is not migrated",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0017",
      "REQ-0020"
    ],
    "alternatives": [
      {
        "option": "Ship `next dev` as the supported configuration",
        "whyNot": "It is cheaper today and permanently unvalidatable. AST-0015 shows the dev and production artifacts differ, so no experiment run against `next dev` says anything about what a consumer receives — the evidence loop this entire product is built around would be measuring the wrong thing forever. It also keeps AST-0010 deferred by keeping the deployment mode undeclared, which is deferral by avoiding the question rather than by judgement."
      },
      {
        "option": "Support both modes as shipped configurations",
        "whyNot": "Two supported configurations are two things to validate, and #96's applicability filter would need every claim scoped by mode — doubling the evidence work to serve a second configuration nobody asked for. A contributor workflow is not a shipped configuration, and naming it one would make it owed."
      },
      {
        "option": "Defer pending discovery on long-running development servers",
        "whyNot": "The question that discovery answers — is a long-lived `next dev` sound — is the wrong question. Even a perfectly stable development server would still be shipping an artifact that differs from the production one. More reading could not move the principle, so spending the search credential here would buy nothing; it is preserved for QST-0018 and QST-0020, where external failure reports can genuinely change the decision."
      }
    ],
    "decidedAt": "2026-08-25",
    "evidencedBy": [
      "AST-0015",
      "AST-0017",
      "AST-0018"
    ],
    "id": "DEC-0018",
    "lifecycle": "active",
    "notes": "⚠️ THIS MAKES AST-0010 OWED, IMMEDIATELY AND KNOWINGLY. Its reopening condition (#137, #80 branch 3) is 'execute before adopting `next build` / `next start`, or when upgrading beyond the tested version' — and BOTH clauses have now fired: this decision adopts production mode, and the version sourced and probed on 2026-08-24/25 is Next.js 16.3.2 against a claim tested at 16.3.1. The cost is accepted rather than discovered: #80's condition fires when the risk becomes POSSIBLE, not when it is observed, because the second means shipping the failure once to learn it was real. ⚠️ It lands at roadmap step 3b (stage 6, risk and feasibility), which is exactly where the roadmap said it would land when it was written — the ordering was not retrofitted to this answer.",
    "rationale": "AST-0015 is decisive on its own: `next dev` and `next build` produce different artifacts, so shipping the development server means shipping a configuration this project can NEVER validate as production — every validation would be measuring something other than what users receive. A shipped configuration should be validated in the mode users receive, and that principle does not depend on how well the development server behaves. AST-0017 establishes the option is actually available: a self-hosted Node.js deployment supports all Next.js features with no platform or adapter, so production mode is reachable for a local-first clonable tool. AST-0018 removed the project-specific objection — a concurrent `next dev` did not measurably interfere with the chokidar watcher — but that argument was never the reason to prefer production, only a reason someone might have preferred dev.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "The application shell's supported configuration is `next build` followed by `next start`. `next dev` remains available to contributors working on the application and is not a shipped configuration: nothing a consumer runs, and nothing the project validates against, is the development server.",
    "title": "The application ships in production mode; `next dev` is a contributor workflow only",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0023",
      "REQ-0016",
      "REQ-0017",
      "REQ-0018"
    ],
    "alternatives": [
      {
        "option": "`io()` from `next/cache` behind a Suspense boundary",
        "whyNot": "It is the documented Cache Components primitive and it is the better-behaved mechanism where it applies — it does not block prefetching. But EVD-0023 measured it FROZEN with cacheComponents disabled, because the documentation is explicit that it resolves immediately in routes without Cache Components. A route built on `io()` would silently serve build-time content if that flag were ever turned off, and nothing in the build output or the served page would say so. Adopting it would also decide the caching model as a side effect of a freshness question, which is precisely the coupling this question was scoped to avoid."
      },
      {
        "option": "`export const dynamic = 'force-dynamic'`",
        "whyNot": "Measured fresh, and the simplest thing that works today. It was REMOVED in Next.js 16.0.0 when Cache Components is enabled, so it is not a contract — it is a mechanism with a known expiry date that the project would have to migrate off the first time anyone enables the flag."
      },
      {
        "option": "`connection()` without a Suspense boundary",
        "whyNot": "Measured fresh with Cache Components disabled, and it FAILS THE BUILD with Cache Components enabled (`blocking-prerender-dynamic`). Loud rather than silent, which is the better failure — but it still makes the contract conditional on a flag."
      },
      {
        "option": "Read planning content from the client instead of the server",
        "whyNot": "It sidesteps the prerender entirely and would work. It also moves every planning-content read out of the Server Component model, which pushes a component-boundary decision this question was explicitly scoped not to make (QST-0018), and it would need a fetch endpoint that does not exist. Available later if the prefetch cost turns out to matter; nothing here forecloses it."
      },
      {
        "option": "Accept the staleness and document it",
        "whyNot": "Not compatible with REQ-0016, REQ-0017 and REQ-0018 as written. Silently serving build-time content violates those requirements rather than raising the risk of violating them, so an accepted-risk signoff would be recording a requirement failure as a tolerance."
      }
    ],
    "decidedAt": "2026-08-26",
    "evidencedBy": [
      "AST-0019",
      "AST-0020",
      "AST-0021",
      "AST-0022",
      "EVD-0023"
    ],
    "id": "DEC-0019",
    "lifecycle": "active",
    "notes": "⚠️ THE PREFETCH LIMITATION IS AN ACCEPTED DESIGN COST, NOT AN OPEN HAZARD, and it is recorded here rather than as a question on purpose. The Next.js documentation states that `connection()` stays suspended until a full user navigation reaches the server, so it also blocks prefetches; `io()` does not. For a local-first tool served over loopback to one operator, correctness of what is on screen outranks the latency a prefetch would have saved. No artifact tracks this as unresolved, because it is not unresolved — it was weighed and paid. If prefetch latency ever becomes a measured problem, that is a new observation that would reopen this decision through supersession, not a deferral sitting open in the backlog today. ⚠️ WHAT THIS DELIBERATELY DOES NOT DECIDE. The Cache Components setting stays undecided — the whole point of the chosen mechanism is that the contract holds either way, so deciding it here would spend the property that was just bought. The MDX component boundary (QST-0018) is untouched: this constrains WHEN a planning-content read may happen, not which component performs it or how the document is rendered. ⚠️ VALIDATION IS OWED AND NOT YET DONE. EVD-0023 measured the mechanism; it has not yet measured the contract as stated above, with `connection()` before each read and a fallback that is a real loading state. Stage 6's `high-severity-risks-mitigated` stays `not-satisfied` until that validation passes — a decision to mitigate is not a mitigation, which is the correction the stage already carries from the AST-0010 round.",
    "rationale": "The deciding property is independence from the Cache Components setting, not freshness. EVD-0023 measured three mechanisms that serve fresh reads, and only this one does so in both caching models: `export const dynamic` does not exist when Cache Components is enabled, `connection()` without a boundary fails the build there, and `io()` behind a boundary is fresh only when the flag is on. AST-0019 established that the freeze itself is unconditional, so the contract that fixes it must be unconditional too. A freshness contract that depends on an unrelated configuration flag is a contract that breaks silently when someone changes the flag — the same failure shape as the build-time freeze it replaces, and harder to see. Choosing `connection()` also keeps this decision inside the freshness contract: the caching model stays undecided, which is where it belongs until the component boundary work reaches it.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "Every server-side read of planning content must await `connection()` from `next/server` before the filesystem read, and that read must sit inside a `<Suspense>` boundary whose fallback is a real loading state for the content being read. This is the artifact-reading contract for REQ-0016, REQ-0017 and REQ-0018. A boundary added only to satisfy the build — one wrapping the whole page, or one whose fallback renders nothing — does not satisfy it: the boundary exists so the static shell can ship while the fresh read streams, and a page-level wrapper makes the whole page the fallback and buys nothing.",
    "title": "Planning-content reads go through `connection()` inside a Suspense boundary",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0018",
      "REQ-0017"
    ],
    "alternatives": [
      {
        "option": "Strip forbidden JavaScript silently, as `next-mdx-remote`'s `blockJS` does",
        "whyNot": "It is the ecosystem default and it is safe in the security sense, which is why it was the original proposal. It fails on a different axis: the author gets no signal, and a stage document that quietly lost a paragraph still renders, still validates, and still ships in the handoff package. That is AST-0028's failure shape arriving through the content pipeline instead of the publisher. Retained UNDER the rejection as defence in depth, never as the contract."
      },
      {
        "option": "Plain markdown for stage documents; MDX only for a fixed set of authored pages",
        "whyNot": "Genuinely viable and materially simpler — no plugin, no compile-time contract, and the CVE class disappears rather than being managed. It loses the component vocabulary in stage documents, which is the reason MDX was proposed: the narrative half of the plan is where a callout, a status chip or a criterion table earns its place. Recorded as the fallback if the per-request compilation cost measured later proves unacceptable."
      },
      {
        "option": "Unrestricted MDX, trusting the agent as the author",
        "whyNot": "It is what MDX's documentation permits — trust your authors — and it is inconsistent with this project's own posture. #88 forbids hand-authoring artifacts precisely because the writer should be a typed operation with a schema behind it; treating the same agent as trusted to write executable documents contradicts that. AST-0028 makes it concrete rather than philosophical: the handoff package ships stage documents verbatim to other people, so unrestricted MDX exports execution to recipients."
      },
      {
        "option": "Keep file-routed `app/**/page.mdx` and accept build-time compilation",
        "whyNot": "Measured and rejected: AST-0030 shows a file-routed document is compiled into the build and does not reflect an edit, which REQ-0018 forbids. `connection()` cannot recover it because there is no request-time read left to make dynamic."
      }
    ],
    "decidedAt": "2026-08-26",
    "evidencedBy": [
      "AST-0029",
      "AST-0030",
      "AST-0031",
      "AST-0023",
      "AST-0026",
      "AST-0027",
      "AST-0028"
    ],
    "id": "DEC-0020",
    "lifecycle": "active",
    "notes": "⚠️ RUNTIME COMPILATION IS FORCED, AND IT IS THE UNCOMFORTABLE PART. AST-0030 rules out file-routed MDX, so a fresh stage document must be compiled per request — which is what `next-mdx-remote` does and where CVE-2026-0969 lived. AST-0031 is what makes that acceptable: the rejection is a property of the plugin, so it holds identically in the runtime path. The restriction is not belt-and-braces here; it is what makes the only viable design defensible. ⚠️ REQ-0021 COVERS THE MAPPED COMPONENTS, and the two contracts help each other. The component implementations reachable from the mapping are ordinary application source, so REQ-0021's static confinement of planning-content reads applies to them exactly as to any other module — a mapped component performing a direct read is the violation REQ-0021 exists to catch. And because imports are refused inside documents, the mapping IS the complete set of components a document can reach, so the confinement check's scope is enumerable rather than open-ended. ⚠️ WHAT IS NOT ESTABLISHED. That the five rejected constructs are every way to reach JavaScript in MDX — six examples cannot show that, and it is the reason stripping stays underneath. That a later rehype or recma stage cannot reintroduce JavaScript. That per-request compilation is affordable at the sizes this project produces. And composing the compile with DEC-0019's `connection()` boundary, which has not been run. ⚠️ CONFIGURATION IS PART OF THE CONTRACT, not incidental: Turbopack is the default builder at 16.3.2 and serialises the config, so the plugin is referenced by ABSOLUTE module path. Both constraints were measured, and both first looked like the contract failing. ⚠️ DOES NOT SETTLE CACHE COMPONENTS. Nothing here touches that flag, and DEC-0019 was chosen so it does not have to be settled. ⚠️ AMENDED 2026-08-26, AND THE AMENDMENT IS THE MECHANISM RATHER THAN A CLARIFICATION. The decision as first recorded named the restriction but left the compilation path to be inferred from AST-0030 in the notes. It is now in the statement: `@mdx-js/mdx` at request time, NOT `@next/mdx` file routing. A reader who took the obvious path — `app/**/page.mdx`, which is what the Next.js documentation shows — would have built something that satisfies REQ-0017 and silently fails REQ-0018, and the decision would have looked satisfied. ⚠️ FOUR ACCEPTANCE OBLIGATIONS ARE CARRIED FORWARD, recorded here and in stage 7 §5. They are NOT `acceptance-criterion` artifacts yet, and that is the schema refusing rather than an oversight: `evaluates` is required with `minItems: 1` and targets `component`, and the application shell has no components until stage 5 runs. A criterion that evaluates nothing cannot be run. They become artifacts at roadmap step 3a. (1) PINNING — the compiler and plugin chain are fixed; changing MDX or adding a remark/rehype/recma plugin reopens security validation. This is a #80 reopening condition and it fires when the change is PROPOSED, not when a document is found to have escaped. (2) REJECTION HOLDS — document-supplied JavaScript stays refused, with stripping underneath as defence in depth. The criterion must check the refusal, not merely the absence of executed code, because stripping alone would also produce no executed code. (3) COST IS MEASURED — per-request compilation cost must be measured, not assumed acceptable. Plain markdown for stage documents remains the fallback, and it is this decision's second recorded alternative rather than a new decision to invent under time pressure. (4) COMPOSITION — runtime reading and compilation must compose with DEC-0019's `connection()` and `<Suspense>` contract. Neither contract has been observed with the other, and REQ-0021's static confinement covers the read that feeds the compiler exactly as it covers any other.",
    "rationale": "AST-0023 established that MDX supplies no allowlist and that its own security model is trusting the author; AST-0026 recorded what that cost someone else, as CVE-2026-0969. AST-0027 showed the ecosystem's answer was to remove the capability rather than filter it. The amendment that shapes this decision is that removal must be LOUD: silent stripping reproduces exactly the failure AST-0028 found in the publisher, where content is absent while the output still looks valid, and this project already refuses that trade in `checkJob` — a shell-string command is refused rather than sanitised, because sanitising means guessing what the author meant. A document is a plan someone will act on; a line that vanished is worse than a build that stopped. Rejection also happens to be the only version that can produce a diagnostic at all: `file.fail` carries the node's position, while a stripping plugin has nothing to report because by construction nothing is wrong afterwards.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "Stage documents are authored in MDX and compiled AT REQUEST TIME with `@mdx-js/mdx` (`compile` then `run`). They are NOT routed as `@next/mdx` file-based pages: that path compiles the document into the build and cannot satisfy REQ-0018. Compilation runs a remark plugin that REJECTS, with a file path and a line:column position, every ESM import or export, every JavaScript expression, every JSX attribute expression, every JSX spread attribute, and every JSX element whose name is not in the permitted component set. Rejection is the user-visible contract: forbidden JavaScript fails the compile and is never silently removed. The permitted set is the component mapping, which is the only route by which a component can reach a document once imports are refused. The compiler and its plugin chain are PINNED, and changing the MDX version or adding any remark, rehype or recma plugin reopens security validation before the change may ship. Stripping may be retained beneath the rejection as defence in depth, but it may never be the observable behaviour.",
    "title": "Stage documents are restricted MDX, compiled at request time; forbidden JavaScript is an error",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0020",
      "REQ-0019"
    ],
    "alternatives": [
      {
        "option": "Put `import 'server-only'` at the top of each Node-only module in `lib/`",
        "whyNot": "The documentation's own example, and what anyone would write first. Measured to break the tool: the package maps `react-server` to an empty module and everything else to one that THROWS on import, so `npm test` and every `bin/` command would fail. It guards the application by breaking the CLI the application exists to serve."
      },
      {
        "option": "No guard; rely on the build failing anyway",
        "whyNot": "It does fail — that is the trap. A check asking only whether the wrong import is prevented reports success here, which is why the Next.js documentation can honestly call the marker optional. The failure carries no file and no position, so the first person to hit it is debugging Turbopack rather than their own import."
      },
      {
        "option": "`export *` from the adapter, so it stays a one-line file per module",
        "whyNot": "It makes the adapter a hole rather than a door. The exposed surface would then grow silently every time `lib/` gains an export, including modules that spawn processes or take locks, and nobody would review the widening because there is no diff to review. Explicit named exports make the surface a decision each time it changes."
      },
      {
        "option": "Reimplement the needed logic inside the application",
        "whyNot": "Contradicts DEC-0017 and #47 directly: one implementation, several callers. It is also unnecessary, since the plain relative import was measured to work."
      }
    ],
    "decidedAt": "2026-08-27",
    "evidencedBy": [
      "AST-0032",
      "AST-0033"
    ],
    "id": "DEC-0021",
    "lifecycle": "active",
    "notes": "⚠️ REQ-0021 GAINS AN EDGE, NOT A SECOND MECHANISM. Its static confinement already has to find planning-content reads outside the approved abstraction; `app/**` importing `lib/**` outside `app/server/` is the same kind of forbidden edge in the same import graph. One check, two rules — and the bypass path is the thing it must detect, because the adapter is a convention until something enforces it and `import '../../lib/x.mjs'` goes straight around the guard. ⚠️ TESTS ASSERT A LOCATABLE FAILURE, NEVER THE WORDING, and this is a measured precaution rather than a style rule. Next.js produced two different messages for the same violation reached two ways, and one of them was WRONG — it said \"you are using it in the Pages Router\" for an App Router project. A test pinned to that string would pass on a wrong message today and break on a correct one after an upgrade. The contract is: the build fails, it names the file, and it carries a line:column. The prose belongs to Next.js. ⚠️ PROCESS-SPAWNING, LOCKING AND STATEFUL MODULES ARE HELD BACK DELIBERATELY. The probe re-exported two pure-ish functions; it did not establish that a lock or a child process survives the adapter, and a Server Component may run concurrently in ways a CLI never does. Each such module is reviewed on its own before it is exposed, so the unexamined case cannot arrive as a side effect of adding a line to an adapter. ⚠️ WHAT IS NOT ESTABLISHED: whether every module the shell eventually needs can be re-exported this way, and whether the unguarded failure mode differs under webpack rather than Turbopack. ⚠️ DOES NOT SETTLE CACHE COMPONENTS. ⚠️ PATH CONFIRMED 2026-08-27 BY TSK-0003: the adapter directory is literally `app/server/`, as written. `src/app/` was tried and Next.js ignored it in favour of the existing root `app/`. So the guarded door will sit beside a file called `app/server.mjs` — the walking skeleton — which are similar names for unrelated things and worth a second's care when reading a diff.",
    "rationale": "AST-0032 measured that the import itself needs nothing: a Server Component reaches a sibling `lib/` module by plain relative path with no alias or bundler configuration, so #47's single implementation stays single for free. What needed deciding was the boundary. A `'use client'` file drags all of its imports into the client bundle (EVD-0046), and while that already fails the build, unguarded it fails as a Turbopack internal error with `location: undefined` and no filename — indistinguishable from a bundler bug. The `server-only` marker changes no runtime behaviour and prevents nothing that was not already prevented; it makes the failure LOCATABLE, which in a tool whose whole premise is legible refusals is the difference that matters. AST-0033 then forced the placement: `server-only` throws when imported outside the RSC compiler, and `lib/` is imported by every `bin/` command and the whole test suite, so the marker cannot live there. The adapter is the only location that guards the app without breaking the tool, and the marker was measured to propagate through a re-export.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "Application code reaches the shared `lib/` modules exclusively through a thin adapter layer under `app/server/`. Every adapter module begins with `import 'server-only'`, before any other import. Adapters re-export by EXPLICIT NAMED EXPORT or wrap the underlying function; blanket `export *` is not permitted. An adapter exposes only the capabilities the shell actually needs, and any module that spawns a process, takes a lock, or holds module-level state requires individual review before it may be exposed at all. No application code outside `app/server/` may import from `lib/` directly. `lib/` itself carries no `server-only` marker, so it remains importable by the CLIs in `bin/` and by the test suite under plain Node. REQ-0021's static check enforces this boundary and detects bypass paths.",
    "title": "The shell reaches `lib/` only through an `app/server/*` adapter carrying `server-only`",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0021",
      "REQ-0018",
      "REQ-0020"
    ],
    "alternatives": [
      {
        "option": "Comment-line keepalive every 15 seconds, as the specification's authoring note suggests",
        "whyNot": "It is the documented idiom and it solves the problem the specification raises — legacy proxies dropping idle connections. It does not solve THIS problem. The client parser ignores comment lines, so no page code can observe one, and the operator's page would remain indistinguishable from a healthy one after the stream died. Correct for its purpose, invisible for ours."
      },
      {
        "option": "Event ids plus `Last-Event-ID` resumption, so a client can recover missed changes",
        "whyNot": "The complete answer to a delta stream, and unnecessary here. Events are hints, not deltas (#73), so there is nothing to replay: a reconnecting client that reloads has the current state by definition. Adding ids would mean maintaining a server-side buffer and a replay path whose only purpose is to reproduce what a reload already achieves."
      },
      {
        "option": "Assert that the watcher starts on `listen` and closes through `server.close()`",
        "whyNot": "True of the walking skeleton and NOT established for the shell. That lifecycle belongs to a `node app/server.mjs` process that owns its own HTTP server; under `next build` + `next start` there is no such object and the process model differs. Writing it into a decision would record an untested claim about a configuration that has never been run."
      },
      {
        "option": "Log watcher failures to the server console and leave the page alone",
        "whyNot": "The operator is looking at a browser, not a terminal. A logged failure with a live-looking page is the retained-workspace-reported-destroyed defect in its purest form: the system knows, records it accurately, and tells the person who needs it nothing."
      }
    ],
    "decidedAt": "2026-08-27",
    "evidencedBy": [
      "AST-0036",
      "AST-0034",
      "AST-0035"
    ],
    "id": "DEC-0022",
    "lifecycle": "active",
    "notes": "⚠️ THE WATCHER LIFECYCLE IS STATED AS AN OBLIGATION, NOT A MECHANISM, and the difference is the point of the amendment. 'The launcher owns both processes and terminates the watcher with the application' is a contract stage 5 must satisfy and validate. The skeleton's create-on-listen / close-in-`close()` pattern is evidence that the shape works for a process that owns its own server; it is not evidence about `next start`, and the two were nearly conflated here. ⚠️ WHAT THE WATCHDOG WINDOW IS, IS NOT DECIDED. The heartbeat interval and the client's tolerance are stage 5 parameters; what is fixed is that a missed heartbeat becomes visible rather than inferred. ⚠️ THE TRUTHFUL-COUNT RULE IS DELIBERATELY ABSOLUTE. 'No rendered count may include an artifact the page did not render' admits no partial-failure exception, because every version of this defect found so far arrived as a reasonable-looking exception: the publisher skipped what it did not recognise, the read path skipped what it could not parse. A count is a claim, and a claim about content that was not rendered is false. ⚠️ NOT ESTABLISHED: browser reconnect behaviour (specification-only; no `EventSource` in this Node build and no browser on this host), idle timeouts beyond six seconds, concurrent clients, and malformation modes other than invalid JSON. ⚠️ DOES NOT SETTLE CACHE COMPONENTS.",
    "rationale": "AST-0034 measured that the skeleton's idle stream and its dead stream are byte-identical and that a change during a disconnect gap is never recovered, which refutes REQ-0018's second clause by construction. AST-0036 then removed the obvious repair: a comment heartbeat is ignored by the client parser, so it fixes the bytes without fixing the visibility — the page stays falsely healthy while something is demonstrably happening on the wire. A named event with a client watchdog is the smallest mechanism the requirement can actually be met with. The reconnect-as-change-hint design survives unchanged and is the part that removes work rather than adding it: because #73 makes events hints rather than deltas, a client that reloads on reconnection is exactly caught up, so the buffering and resumption machinery that a delta stream would need is unnecessary. AST-0035 supplies the read-path half: a corrupt artifact currently vanishes while the header still counts it, which tells the operator the missing thing is present.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "The application is started by a documented launcher that owns both the application process and the file watcher, and that terminates the watcher with the application. The mechanism binding them is stage 5's to choose and to validate; nothing is claimed today about how the watcher's lifetime is enforced under `next start`. The update stream emits a NAMED heartbeat event carrying data at a fixed interval, and the client runs a watchdog that renders a visibly disconnected state when a heartbeat fails to arrive within its window; comment-only keepalives are not sufficient because the client parser ignores them. A watcher failure must either emit a visible failure signal on the stream or close the stream — logging alone is not permitted, because a page whose watcher has died otherwise stays live-looking and stale. On reconnection the client reloads, treating reconnection itself as a change hint; no event buffer, event id or `Last-Event-ID` handling is required, because events are hints and never deltas. A malformed or unreadable artifact must be surfaced visibly and located, and no rendered count may include an artifact the page did not render.",
    "title": "The shell's lifecycle contract: a launcher owns both processes, and stream health is a visible event",
    "type": "decision"
  },
  {
    "addresses": [
      "QST-0019",
      "REQ-0016"
    ],
    "alternatives": [
      {
        "option": "Start with the assertion/evidence view, which already has a working implementation",
        "whyNot": "The cheapest view to reach and the wrong one to start with. DEC-0017 keeps `app/server.mjs` as the verified substrate reference until the shell reaches parity, so that view HAS a working home today — building it again first would spend the slice re-achieving something that already works while REQ-0016 and REQ-0017 stay unaddressed. It is an argument about order, and the order it argues for is later."
      },
      {
        "option": "A single combined view: current stage and its document on one page",
        "whyNot": "Fewer routes, and it collapses navigation into the thing being navigated. Nine stages need somewhere to be listed with their gate state, and REQ-0016 is about the project's position rather than one stage's content. Merging them would also put the derived stage list behind the per-request MDX compile, paying an unmeasured cost to render something that does not need it."
      },
      {
        "option": "Add a tracker or dashboard to the first slice",
        "whyNot": "No requirement in REQ-0016..REQ-0019 asks for one, and DEC-0022's truthful-count rule binds every summary surface — so a tracker is not a view, it is a view plus an obligation to prove every total it prints. Deferred until something demands it (#106)."
      },
      {
        "option": "Permit client-held view state for interface smoothness",
        "whyNot": "Rejected as stated, and permitted as amended. Blanket 'no client state' was too strong — transient interface state is harmless and forbidding it would be a rule nobody could follow. What may not live only in the client is anything whose loss is a correctness problem, because the reload contract will destroy it without warning and the user will not know what they lost."
      }
    ],
    "decidedAt": "2026-08-27",
    "evidencedBy": [
      "AST-0034",
      "AST-0035"
    ],
    "id": "DEC-0023",
    "lifecycle": "active",
    "notes": "⚠️ THE STATE RULE IS THE LOAD-BEARING CLAUSE, and its wording was corrected during approval. 'No client state' is unenforceable and wrong — a disclosure triangle holds client state. The rule is about CONSEQUENCE: DEC-0022 reloads the page on every content change and every reconnection, so any state held only in the client is state the system will silently discard at a moment the user did not choose. Transient interface state survives that fine because losing it costs nothing. Filters, a selected section, unsaved work and navigational context do not, so they are URL-encoded or persisted. ⚠️ DEFERRED IS NOT REMOVED, and the distinction is recorded because deferral silently becoming deletion is #107's failure. The artifact tracker and the assertion/evidence view remain in the roadmap at step 5; the latter has a working reference implementation in the skeleton, which is why it can wait rather than why it is unnecessary. ⚠️ WHAT WOULD CHANGE THIS. If per-request MDX compilation is measured expensive (DEC-0020's third obligation), the stage view is where it lands, and the fallback is that decision's own second alternative — plain markdown — not a different information architecture. If reload-on-change proves too coarse in use, the answer is a finer change signal, not client-held correctness state: the reload contract is what makes the missed-change defect impossible. ⚠️ DOES NOT SETTLE CACHE COMPONENTS. Nothing in this architecture depends on that flag, which is the property DEC-0019 was chosen to preserve and the fourth decision in a row to preserve it.",
    "rationale": "This is a product decision and no source settles it; sequencing it fifth bought elimination rather than evidence. DEC-0022 makes every content change and every reconnection a full page reload, which is the sharpest constraint and the one that would have been discovered last — it decides what state a view may hold far more than any preference about views does. DEC-0019's blocked prefetching argues for fewer, denser views, an inference from an accepted cost rather than a measurement. DEC-0020's per-request compilation cost is explicitly unmeasured, which caps the design at one stage document per view. DEC-0021 makes read-only views cheap while the review-status write is the one locking capability the slice must get individually reviewed and exposed. Two views is what remains once those four have spoken, and it satisfies REQ-0016 through REQ-0019 without adding a surface the truthful-count rule would then have to cover.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "The application shell opens with exactly two views. `/` is the project view: the derived current stage, navigation across all nine stages with their gate state, artifact counts under the truthful-count rule, and lint findings surfaced rather than re-judged. `/stage/[stageId]` is the stage view: the working stage document rendered as restricted MDX, the stage's exit criteria with their recorded attestations, and review actions through the typed write path. Both views are URL-addressable and server-rendered. NO CORRECTNESS-CRITICAL STATE IS HELD ONLY IN THE CLIENT: transient interface state is permitted, but anything a user must not lose — filters, a selected section, unsaved work, navigational context — is URL-encoded or persisted. The artifact tracker and the assertion/evidence view are DEFERRED, not removed.",
    "title": "The first slice is two views: a project view and a stage view",
    "type": "decision"
  },
  {
    "addresses": [
      "REQ-0017"
    ],
    "alternatives": [
      {
        "option": "Read the stage definitions in the route entry and 404 before streaming",
        "whyNot": "It would work and it would put a planning-content read outside the reader, in a route entry with no boundary above it — the exact violation `lint:shell` reports as `read-in-route-entry`. Weakening the read boundary to improve a status code trades a checked invariant for an unchecked convenience."
      },
      {
        "option": "Hard-code the known stage ids in the route so it can validate without reading",
        "whyNot": "A second registry of the same fact, free to drift from the definitions on disk. It would fail in the direction that looks fine: a newly added stage would 404 while its document sits there, and nothing would say why."
      },
      {
        "option": "Render the whole page without streaming so the status stays available",
        "whyNot": "It gives up the independent boundaries ACC-0016 requires, so a slow document read would hold the criteria hostage. Trading a real property of the page for a property of its envelope."
      }
    ],
    "decidedAt": "2026-08-28",
    "evidencedBy": [
      "AST-0039"
    ],
    "id": "DEC-0024",
    "lifecycle": "active",
    "notes": "⚠️ WHAT IS ACCEPTED IS THE STATUS, NEVER THE SILENCE. The same streaming constraint produced AST-0039 — a refused document arriving as 200 and an opaque digest — and that was NOT accepted: ACC-0036 requires the refusal to be visible, located and escaped. The rule these two share is that a 200 with a truthful body is fine and a 200 with a blank region is not. ⚠️ THE REOPENING CONDITION IS A CONSUMER, NOT A DATE. Nothing about this is right in general; it is right for a page a person reads. The moment something parses the response rather than reads it — a crawler, a health check, an API client — the status becomes load-bearing and this comes back. #80's rule applies: the trigger fires when such a consumer is PROPOSED, not once one has been misled.",
    "rationale": "The status is committed before the answer is known. `notFound()` runs inside a `<Suspense>` child, because the read that would reveal the id to be unknown may only happen there: DEC-0019 confines planning-content reads to the reader behind a boundary, and REQ-0021 makes that confinement a statically checked rule. By the time the stage is known not to exist, the shell has streamed and the status line is gone. The two ways to recover a 404 both cost more than it is worth here — validating in the route entry means reading outside the boundary, which the check refuses by design; keeping a separate list of stage ids in the route means a second registry that can silently disagree with the definitions on disk, which is the stored-derived-state failure #96 exists to prevent. This is a local, human-facing application served over loopback to one operator, and an operator reads the page rather than the status line.",
    "reviewStatus": "draft",
    "schemaVersion": 2,
    "statement": "When `/stage/[stageId]` is given an id matching no stage, the application responds 200 and renders a visible not-found state. It does not return 404. The body is required to be truthful; the status is not. This is revisited if an API, a crawler, or any automated client comes to depend on HTTP status semantics for this route.",
    "title": "An unknown stage returns HTTP 200 with a truthful body — accepted for v1",
    "type": "decision"
  }
]
