{
  "stageId": "06-risk-feasibility",
  "attestations": {
    "high-severity-risks-mitigated": {
      "result": "not-satisfied",
      "decidedBy": "pm",
      "reason": "AMENDED 2026-08-25, AND THE AMENDMENT CORRECTS THIS ATTESTATION'S OWN EXIT CONDITION. The previous reason said this returns to satisfied when AST-0010 is validated across both configurations, or on an accepted-risk signoff. Both halves were wrong, and the validation is what showed it. ⚠️ VALIDATION CONFIRMED THE RISK; IT DID NOT MITIGATE IT. EVD-0022 measured both configurations under next build + next start at 16.3.2: the route prerenders static and serves its build-time value in BOTH, so a later external write changes nothing a fresh request returns. AST-0019 records that at rung 4, environment-matched. Measuring a hazard precisely is not the same as having a mitigation for it — and this measurement removed the mitigation people would have reached for first, because disabling Cache Components does not help. ⚠️ AN ACCEPTED-RISK SIGNOFF IS NOT AVAILABLE HERE, and that is a fact about the requirements rather than about appetite. REQ-0016, REQ-0017 and REQ-0018 require the application to display current stage state, render a stage document, and reflect external content changes without a manual reload. Silently serving build-time content does not make those requirements riskier — it VIOLATES them. A risk that can be accepted is one where the plan still holds if it lands; here the plan does not. ⚠️ RETURNS TO SATISFIED when QST-0023 produces a production-safe design AND that design is validated — an artifact-reading route measured to reflect an external write under next build + next start. Not on the design being chosen, and not on the hazard being understood: this criterion asks for a mitigation, and a mitigation that has not been run is a plan for one."
    },
    "load-bearing-assertions-at-rung": {
      "result": "n/a",
      "decidedBy": "pm",
      "reason": "RE-EVALUATED 2026-08-25 rather than carried forward, because the plan it was evaluated against has changed. The scope is DERIVED FROM GRAPH USE — assertions that active instructions rest on — and not from the optional `loadBearing` flag (TSK-0002). Measured again on the live project: the scope is still EMPTY. The only runbook-step, RBS-0001, is `retired` and rests on AST-0002; no active instruction rests on anything at all. ⚠️ AST-0010 is NOT captured by this criterion, and that is the point of checking rather than assuming: it is owed, it is load-bearing, and nothing beneath an active instruction depends on it yet — so this criterion has nothing to say about it. The risk is carried by `high-severity-risks-mitigated`, which is now not-satisfied, and stage 6 blocks there. A criterion stretched to cover a risk it does not describe would make both of them harder to read. ⚠️ Recorded n/a rather than satisfied for the same reason as before: an empty scope means there was nothing to reach a rung, which is not the same as everything having reached one."
    }
  }
}
