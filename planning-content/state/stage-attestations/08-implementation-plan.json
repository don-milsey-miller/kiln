{
  "stageId": "08-implementation-plan",
  "attestations": {
    "tasks-trace-to-requirement": {
      "result": "satisfied",
      "decidedBy": "pm",
      "reason": "RESTORED 2026-08-27 after review, the amendments, and a re-check of the whole graph. All seventeen active tasks carry a non-empty `fulfils`, a non-empty `implements` and a non-empty `acceptedBy`; every run-2 acceptance criterion (ACC-0013..ACC-0035) is claimed by at least one task; and each of CMP-0012..CMP-0020 is implemented by at least one task. Measured on the live project rather than asserted. ⚠️ TSK-0003 KEEPS ACC-0033 EVEN THOUGH TSK-0013 NOW OWNS IT, and the reason is this criterion. Stripping the trace to make ownership exclusive would have left a task with no acceptance link at all — the criterion asks that every task trace, not that every criterion have one owner. The scaffold is a necessary condition for install-and-start; TSK-0013 is where it is satisfied, and both notes say so. ⚠️ REGRESSES again whenever tasks are authored and not judged against this criterion, which is how it regressed earlier today."
    },
    "tasks-developer-sized": {
      "result": "satisfied",
      "decidedBy": "pm",
      "reason": "SATISFIED 2026-08-27, after three tasks were split and one architectural leak was removed. This is the first time this criterion has had a real answer: it was `n/a` while `task` was not yet a type, and not-satisfied while twelve unreviewed tasks sat under it. ⚠️ THE THREE SPLITS WERE THE SUBSTANCE OF THE REVIEW. TSK-0006 became an `app/server` → `lib/` import check plus a separate planning-read and `<Suspense>` check — the halves have different prerequisites, one needing only the adapter and the other needing the reader, so bundling them delayed the cheaper one for no reason. TSK-0007 became stage navigation plus the truthfulness rules, which fail in different ways: navigation fails visibly and a wrong count fails silently. TSK-0011 split along the wire into what is SENT and what is OBSERVED, which is exactly the distinction AST-0036 turned on. ⚠️ ONE LEAKED ARCHITECTURE WAS REMOVED, and it is worth recording how it got in. TSK-0003 configured `@next/mdx`, an absolute remark-plugin path and Turbopack's serialisation constraint — all of which belong to FILE-ROUTED MDX, the option DEC-0020 explicitly rejected after AST-0030 measured it compiling documents into the build. Those settings came from the PROBE that measured the losing alternative, and the probe's incidental setup was carried into the plan as though it were the design. ⚠️ ORDERING REVIEWED AND APPROVED: both boundary checks complete before any view work — verified transitively for TSK-0007, TSK-0016, TSK-0009 and TSK-0010 — and the review action no longer waits on the change stream, so the first write path and the streaming work proceed independently. The benchmark runs after the stage view, in parallel with later interaction work. ⚠️ WHAT THIS DOES NOT SAY: that any task has been done. Every acceptance criterion for CMP-0012..CMP-0020 remains `not-evaluated` and every one of those components has an absent `implementedBy`. This criterion is about the plan being workable, not about the work being done."
    }
  }
}
