{
  "stageId": "07-acceptance-criteria",
  "attestations": {
    "criteria-objective": {
      "result": "satisfied",
      "decidedBy": "pm",
      "reason": "⚠️ RE-EVALUATED 2026-09-03 OVER 94 ACTIVE CRITERIA, counted from the graph. The previous reason said 88, which was true when written and stopped being true as soon as 6 more were authored — ACC-0090, ACC-0091, ACC-0092, ACC-0093, ACC-0094, ACC-0095 — without this record being touched. Found by sweeping every attestation for hardcoded totals after two Stage 9 reasons went stale the same way; the sweep is the durable part, not this correction. Current outcomes: 52 not-evaluated, 42 pass. ⚠️ THE SIX NEW ONES ARE WHERE OBJECTIVITY WAS HARDEST, and each names an observation someone could make rather than a quality someone could feel. ACC-0094 requires each repaired link to clear a NAMED lint finding that the fixture first proves exists. ACC-0095 requires the trace guard to be enumerated FROM the schemas rather than compared against a list in the test — a criterion that permitted the list would have re-created the defect one layer up. ACC-0091 excludes fabricated OAuth credentials in its statement, because that is the cheap way to make it pass while proving a storage shape. ACC-0090 records Pi's no-TTY behaviour as OBSERVED rather than as required, since an observation that only passes when the runtime behaves as hoped is not an observation. ACC-0093 requires the login process never to be SPAWNED rather than a message to be printed, because a refusal printed just before launching anyway would satisfy a text assertion and still hang. ACC-0092 requires both presence AND drift detection, since presence alone does not show a consumer would notice an absence. ⚠️ THREE OF THE SIX ARE ALREADY EVALUATED — ACC-0094 and ACC-0095 pass, and ACC-0042 was narrowed to the resolver it actually tests when its setup-level half turned out to live in ACC-0085. The other three are correctly not-evaluated: they belong to work that has not run."
    }
  }
}
