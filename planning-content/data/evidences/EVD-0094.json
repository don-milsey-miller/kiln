{
  "id": "EVD-0094",
  "type": "evidence",
  "schemaVersion": 2,
  "reviewStatus": "draft",
  "lifecycle": "active",
  "title": "Measurement: provider ids that name Object.prototype members are refused cleanly and resolve when declared, both platforms through CI",
  "kind": "experiment",
  "summary": "Supplemental to EVD-0092 for ACC-0058: the correction to provider ids that name Object.prototype members, observed on both platforms through CI. GitHub Actions run 34540859311 at commit b648e3e, matrix ubuntu-latest x windows-latest x node 22 (v22.23.2) and 24 (v24.20.0), all four cells green: 840 tests, 0 failures in every cell. All TWENTY-FOUR provider-contract tests ran in every cell, checked by extracting each name from test/pi-provider-credentials.test.mjs and matching it against each cell's log, with zero failing-test lines anywhere in any log. WHAT CHANGED AND WHAT DID NOT. Before b648e3e, both credential tables were read with a bracket lookup on a plain object, which also finds what Object.prototype holds. An id such as `toString` resolved to an inherited function, which is truthy, so it was refused as a DECLINED provider: detail.why held the function and the message printed its source. EVD-0092's claim that an unknown provider is refused was therefore already true for these ids - they were refused - and this record does not contradict it. What was wrong was the refusal's content, and that the same lookup in the built-in table would have returned the inherited function as a contract had the declined check not run first. Both lookups now ask Object.hasOwn, and nothing was added to the declined table. THE THREE TESTS, each passing in all four cells: (1) `F35 every Object.prototype name is refused as an ordinary unknown provider` - without a custom declaration, each name receives the ordinary unsupported-credential-contract refusal whose detail is exactly { provider }, with no `why`, plain data that survives a JSON round trip unchanged, and a message carrying no function source, no `[native code]` and no `[object`. (2) `F35 a prototype-named id with a valid custom declaration resolves like any other custom provider` - with a valid matching declaration, each name resolves to that declared contract, its id, the custom-environment-key source and the declared variable. This is what shows the fix is an own-property lookup rather than a denylist: a fix that refused these names outright would pass test 1 and fail this one. (3) `F35 the two declined providers keep their recorded reason, and built-ins resolve unchanged` - amazon-bedrock and azure-openai still carry their recorded reason, unchanged, in both detail.why and the message; the declined table still holds exactly those two ids; and every built-in provider resolves to its same frozen table entry. Three mutations were run locally before the commit and each fails tests 1 and 2: restoring the raw lookup in the declined table, restoring it in the built-in table, and the resolver exactly as committed before this correction.",
  "outcome": "success",
  "observedAt": "2026-09-10",
  "environment": {
    "execution": "host",
    "isolationBoundary": {
      "isolates": [],
      "doesNotClaim": [
        "containment-of-hostile-code",
        "host-filesystem-denial",
        "network-isolation",
        "process-isolation"
      ]
    },
    "facts": {
      "ci": "GitHub Actions run 34540859311",
      "commit": "b648e3e",
      "matrix": "ubuntu-latest and windows-latest, node 22 and node 24 - all four cells green",
      "cell-ubuntu-22": "job 103082967676, node v22.23.2, success, 840 tests / 837 pass / 0 fail / 3 skipped",
      "cell-ubuntu-24": "job 103082967955, node v24.20.0, success, 840 tests / 837 pass / 0 fail / 3 skipped",
      "cell-windows-22": "job 103082967894, node v22.23.2, success, 840 tests / 839 pass / 0 fail / 1 skipped",
      "cell-windows-24": "job 103082967902, node v24.20.0, success, 840 tests / 839 pass / 0 fail / 1 skipped",
      "provider-tests-per-cell": "24 of 24 matched by name in every cell; zero failing-test lines in any log",
      "prototype-name-tests": "F35 every Object.prototype name is refused as an ordinary unknown provider; F35 a prototype-named id with a valid custom declaration resolves like any other custom provider; F35 the two declined providers keep their recorded reason, and built-ins resolve unchanged - all three passing in all four cells",
      "prototype-name-bound": "each passing runner iterated its complete own Object.prototype property name set, which contained at least 12 names",
      "unknown-refusal-shape": "detail exactly { provider }, no why, JSON-safe, no function source or [native code] or [object in the message",
      "custom-resolution": "a valid matching custom declaration resolves a prototype-named id to that contract",
      "unchanged": "declined table holds exactly amazon-bedrock and azure-openai with their recorded reasons; built-ins resolve to their frozen entries",
      "lookup": "Object.hasOwn on both UNSUPPORTED_PROVIDERS and PROVIDER_CREDENTIALS; no names added to the declined table",
      "local-mutations": "3 of 3 killed before commit, each by tests 1 and 2: raw lookup restored in the declined table, raw lookup restored in the built-in table, the resolver as committed before this correction",
      "readiness-flake-R7": "the readiness test passed in all four cells",
      "relation-to-EVD-0092": "supplemental: EVD-0092's refusal claim held before this fix; this record covers the refusal's content and custom resolution",
      "under-test": "lib/pi-provider-credentials.mjs resolveProviderCredentials"
    },
    "omissions": [
      {
        "fact": "per-cell-prototype-name-list-and-count",
        "state": "not-captured",
        "reason": "The tests iterate the complete set of own property names Object.prototype has on each runner's runtime, but the CI logs do not retain which names that set held or how many there were in each cell. A passing test establishes only a bound: each runner exercised its complete runtime set, and that set contained at least 12 names. The exact list and count were observed locally on one runtime and not recorded per cell."
      },
      {
        "fact": "mutation-controls-in-ci",
        "state": "not-captured",
        "reason": "The three mutations - either raw lookup restored, and the pre-correction resolver - were run locally before the commit and not in CI. CI establishes that the prototype-name tests pass on both platforms; that they fail against the defect is established only by the local runs."
      },
      {
        "fact": "macos",
        "state": "not-captured",
        "reason": "The CI matrix is ubuntu-latest and windows-latest. macOS is not built, not tested and not claimed."
      }
    ]
  }
}
