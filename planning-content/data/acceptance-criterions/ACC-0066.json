{
  "id": "ACC-0066",
  "type": "acceptance-criterion",
  "schemaVersion": 2,
  "reviewStatus": "draft",
  "lifecycle": "active",
  "outcome": "not-evaluated",
  "title": "Packaged skills are generated, and staleness fails a check",
  "statement": "The generator produces exactly nine packaged stage skills, one per canonical stage definition - `kiln-stage-01-intake`, `kiln-stage-02-intent-decomposition`, `kiln-stage-03-discovery`, `kiln-stage-04-requirement-gaps`, `kiln-stage-05-solution-design`, `kiln-stage-06-risk-feasibility`, `kiln-stage-07-acceptance-criteria`, `kiln-stage-08-implementation-plan`, `kiln-stage-09-handoff` - each at `pi-package/skills/<name>/SKILL.md` with that name in its frontmatter, and each carrying its definition's decision owner, its outputs - the typed artifacts, the outputs not yet typed and the output prose - and its exit criteria. The output prose is rendered as output prose and never relabelled as the stage's purpose. Generation is deterministic: the same definitions produce byte-identical files on every run and on every platform, with no timestamp and no machine-specific content. The check mode writes nothing, and fails naming each skill by name that is stale (its content differs from what the definitions generate), missing (a definition has no generated skill) or orphaned (a generated skill has no definition). The check runs in CI on Windows and on Ubuntu. The handwritten `kiln-planning` skill is neither adopted nor removed by generation or by the check.",
  "evaluates": [
    "CMP-0033"
  ],
  "verifies": [
    "REQ-0001"
  ],
  "notes": "⚠️ SCOPED 2026-09-13 TO WHAT TSK-0046 PROMISES, AND NO FURTHER (O1, then F88). The criterion accepts the decision owner, the outputs and the exit criteria - the three things a canonical definition carries - together with the nine identities and their paths, byte determinism, the three drift classes by name, a check that writes nothing, and both CI platforms. Purpose, method, next activity, delegations, boundaries and the completion summary are accepted by ACC-0112 instead, because their canonical fields do not exist yet. ⚠️ OUTPUT PROSE IS NOT A PURPOSE (F88). `producesProse` says what a stage emits. A skill that printed it under Purpose would tell an agent what the stage is for on the strength of a field that never said so - the drift CMP-0033 exists to prevent, arrived at by relabelling rather than by duplication. ⚠️ MISSING AND ORPHANED ARE STALENESS TOO. A definition added without regenerating, and a definition removed while its skill stays packaged, are the same drift as an edited one; a check that only compared the files present on both sides would pass both. ⚠️ BYTE DETERMINISM IS WHAT MAKES THE CHECK MEAN ANYTHING. A generator whose output varies with line endings, key order or the clock makes every check stale on some platform, and a check that is always stale is one nobody reads."
}
