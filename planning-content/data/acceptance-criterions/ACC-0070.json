{
  "id": "ACC-0070",
  "type": "acceptance-criterion",
  "schemaVersion": 2,
  "reviewStatus": "draft",
  "lifecycle": "active",
  "outcome": "not-evaluated",
  "title": "The orchestrator cannot cross a user-owned gate",
  "statement": "An attempt by the orchestrator to attest or advance a stage exit criterion, to set an artifact's review status to `approved`, or to activate or deactivate an artifact type is refused unless the operator confirmed that exact action through Kiln's interactive confirmation channel during that same tool invocation. No model-supplied argument can carry an authorisation value or actor identity. After confirmation, Kiln supplies its own internal actor string to the underlying operation; any actor identity that operation records therefore comes from Kiln, not the model. The confirmation result does not outlive the invocation that obtained it. Where no confirmation channel is available, or the operator cancels, rejects, or does not answer, the tool refuses with a stable refusal code. On a refusal the target attestation file, artifact file and type manifest are unchanged in bytes and in modification time, and a bounded refusal record naming the operation, the refusal code and the target identifiers is persisted and reported by `kiln_project_status`.",
  "evaluates": [
    "CMP-0034"
  ],
  "verifies": [
    "REQ-0001"
  ],
  "notes": "Amended 2026-09-18 under TSK-0050, before implementation, and again the same day (F18). The original statement named Stage 1 attestation alone. The boundary is the same act for artifact approval and for type activation and deactivation, so all three are stated here rather than left to the implementation to generalise. Material-change preview is a separate criterion, ACC-0115: that one is the orchestrator's behaviour before a mutation, where this one is the tool boundary that holds whatever the orchestrator does.\n\nAuthorisation is Pi's interactive confirmation channel, `ctx.ui.confirm`, called with the invocation's own signal and a 30,000 ms timeout. Only a literal `true` authorises the action. No UI context, rejection, cancellation, timeout, abort and an unexpected UI error all refuse and all write the bounded refusal record. They share one refusal code, `operator-confirmation-not-granted`, because Pi returns the same `false` for every one of them: with no UI bound `confirm` is `async () => false`, and in RPC mode a cancelled dialog and an expired timeout both resolve `false` through the same path, which is why the SDK's own timed-confirm example labels the result \"Cancelled or timed out\". A code claiming to separate them would be a distinction Kiln invented rather than observed. An unexpected UI error refuses without the raw error reaching the model or the record.\n\nF18: what each operation DOES with Kiln's actor string differs, and this criterion claims only what is true of all three. All three receive `operator via Pi UI`. Attestation persists it as `decidedBy`, and activation persists it in the project manifest. Review status persists the `approved` status and returns the actor to the caller, but stores no approver: the artifact envelope in `schemas/common.schema.json` has no reviewer field. Adding one, or an approval history, is an artifact-schema and migration decision that TSK-0050 does not make, so neither this criterion nor the confirmation dialog claims durable attribution for an approval.\n\nThe refusal record is `state/operator-boundary-refusals.json`, bounded at the newest 100 entries and holding only validated identifiers and enums. Evidence is split: deterministic tests cover both the confirmed and the refused branch, including an injected UI context for the confirmed one, because a harness that answered the dialog would be acting as the operator. The retained RPC run leaves the dialog unanswered and shows refusal after the bound."
}
