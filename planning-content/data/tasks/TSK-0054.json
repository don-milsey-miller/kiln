{
  "id": "TSK-0054",
  "type": "task",
  "schemaVersion": 2,
  "reviewStatus": "approved",
  "lifecycle": "active",
  "title": "Build capability-signature verification and the refusal matrix",
  "statement": "Verify the child's Kiln capability signature before reading its substantive answer, and cover each refusal cause with a test, including the fake child that returns plausible prose with none of Kiln's tools. This is the detection half of decision #67 that has never been built.",
  "role": "platform",
  "implements": [
    "CMP-0036"
  ],
  "fulfils": [
    "REQ-0026",
    "REQ-0025",
    "REQ-0024"
  ],
  "acceptedBy": [
    "ACC-0075"
  ],
  "dependsOn": [
    "TSK-0053"
  ],
  "notes": "Scope settled 2026-09-20, before implementation. ACC-0075 needs no amendment: it already names all eight causes, and this task builds the detection for them.\n\nTHE MATRIX AUDIT FOUND THREE CAUSES WITH NO CHECK AT ALL. A nonzero exit is captured as `run.exit` and never consulted. A line that fails `JSON.parse` is silently skipped, so a child emitting garbage around one valid assistant message is accepted. And `CHILD_REFUSED.OUT_OF_ROLE_WRITE` is defined with `mayWrite` implemented and no production caller anywhere, so nothing observes a write outside a role's boundary. It also found that answer extraction runs BEFORE verification: `answerFrom(stdout)` is called inside `runChild` while the gate runs after it, so the assistant text is selected before anything is checked. The refusal path drops it today, which is why the criterion is not violated, but the ordering makes that incidental rather than structural.\n\nD47 TWO NEW WIRE CODES, EVERY EXISTING LITERAL PRESERVED. `NONZERO_EXIT: \"child-exited-nonzero\"` and `EVENTS_MALFORMED: \"child-events-malformed\"`. \"The child did not finish\" and \"the child's stream could not be read\" are different facts from the five already coded, and a caller that cannot tell them apart cannot act on either. The pinned wire-contract test in test/specialist-contract.test.mjs is extended rather than replaced.\n\nD48 A SELECTION MISMATCH IS NOT A MISSING CAPABILITY. `SELECTION_MISMATCH: \"child-selection-mismatch\"`. A child answering under a provider or model nobody asked for fails for a reason a delegating model would otherwise be told was a missing tool, and would look for the wrong fix. A missing or malformed report still resolves to `capability-missing`, because in that case the child genuinely cannot demonstrate its tools.\n\nD49 ABSENCE STAYS PRIMARY; OBSERVATION IS A FAIL-SAFE. `--tools` and the measured active registry keep an out-of-role tool unavailable, which is the boundary #67 asks for and the one a child cannot argue with. The package extension is NOT made role-aware: that would put a second, weaker copy of the write boundary inside the child and would change a launch protocol that was just measured. Instead Pi's own `tool_execution_start` events are parsed, and an out-of-role mutation that nevertheless STARTS is a refusal. Starting is sufficient: a handler that failed may already have partially mutated state, so waiting for `tool_execution_end` would be waiting for damage to finish. The stable code remains `out-of-role-write`.\n\nNOT READ, OPERATIONALLY. Rejected assistant prose is never selected as the answer, returned, logged or included in refusal details. Buffering process stdout is permitted and necessary: malformed-event detection requires reading the stream. `answerFrom` moves after acceptance so the ordering enforces it.\n\nPRECEDENCE. A run can fail several ways at once, so the order the checks run in is itself a contract and gets its own test: timeout or abort, then malformed events, then nonzero exit, then the task binding, then the selection, then out-of-role writes, then capabilities and signatures. Cheapest and most fundamental first, and nothing about the child's own account is consulted before the facts the process itself supplies.\n\nFIVE REVIEW CORRECTIONS, APPLIED BEFORE THE MUTATION GATE. F39 the binding now refuses at its own step rather than waiting for `verifyChild`, so a child that was never given the task is never refused for a wrong model or a write instead. F40 the write boundary is `mayWrite` over the canonical operation a wire name resolves to, not membership of the allowlist: `active` is the role's tools intersected with the host registry, so a host missing a tool would otherwise make a declared write look out of role, and a read the role simply was not given would be reported as an attempted mutation. F41 a `toolName` this host does not register makes the stream unreadable and is never copied anywhere; every name in an observation comes from Kiln's own table. F42 a type is not a shape: the event types the gate consumes are checked for the fields it reads, and the answer and the native selection are taken only from `message_end` and `turn_end`. The shapes are measured, not assumed - a real child run showed `message_update` carries no `message` at all, which a guessed rule refused. F43 the matrix fixture takes a LIST of faults, so every adjacent pair in the precedence order is a genuine double failure; `bindingDetail` is gone from observations because it quotes the child's own material, and `durationMs` is restored and read when each observation is built."
}
