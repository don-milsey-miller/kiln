---
name: "kiln-stage-09-handoff"
description: "Kiln planning stage 09, Developer Handoff: its purpose, method, next activity, allowed delegations, mutation and approval boundaries, completion summary, decision owner, outputs and exit criteria as the canonical stage definition declares them. Use when the project's current stage is 09-handoff."
---

<!-- Generated by lib/stage-skills.mjs from stages/09-handoff.json. Do not edit this file: edit the stage definition and regenerate. -->

# Stage 09 — Developer Handoff

## Purpose

Hand each assigned person a slice they can start from without coming back to ask.

## Decision owner

—

## Outputs

The exported package, sliced per role, including the runbook and its evidence.

Typed artifacts this stage produces:

- `runbook`
- `runbook-step`

Outputs the definition names that have no artifact type yet:

- the exported package
- sliced per role

## Exit criteria

- `role-slice-self-sufficient` — Each assigned team member can start their part without coming back to ask a question. (evaluated by a person and recorded as an attestation)
- `runbook-steps-above-threshold` — Every runbook step rests on an assertion at or above the threshold (#57). (evaluated by a person and recorded as an attestation)
- `no-unresolved-critical-questions` — No unresolved critical questions remain. CRITICAL, per DEC-0014: the question blocks active handoff content, or makes a handoff criterion unevaluable. (evaluated by a person and recorded as an attestation)

## Method

Slice per role, rest every runbook step on an assertion at or above threshold, and clear the critical questions.

1. Read the task set, the acceptance criteria and the evidence behind them.
2. Author the runbook and one step per action, each resting on an assertion at or above the threshold (#57).
3. Delegate to the planning specialist where a slice needs its steps written for a role.
4. Slice the package per assigned role and check that each slice stands on its own.
5. Resolve every critical question, or ask the operator to settle it (DEC-0014).
6. Link each step's evidence to the step it supports.
7. Ask the operator to confirm the attestation, record an attestation against each exit criterion, then exit.

## Next activity

Resolve a critical question first; delegate to planning when a role slice needs its steps written; author or revise while any slice is incomplete or any step rests below the threshold; attest once every exit criterion holds; exit only after attestation.

Activities this stage may choose between, in order:

- `question`
- `author`
- `delegate`
- `attest`
- `exit`

Always:

- A step resting on an assertion below the threshold does not ship (#57).
- A question that blocks active handoff content, or makes a handoff criterion unevaluable, is critical (DEC-0014).

## Delegations

- `planning` — requires `[]`

## Boundaries

This stage may perform these non-creation operations:

- `linkEvidence`
- `linkTrace`
- `resolveQuestion`
- `reviseArtifact`
- `setLifecycle`
- `unlinkEvidence`
- `unlinkTrace`
- `writeStageAttestation`
- `writeStageDocument`

It must not touch:

- A task's statement.
- An acceptance criterion.

Approving this stage approves: That each role slice can be started without coming back to ask.

Approval requires:

- The operator confirms the exact stage attestation or advance through Kiln's interactive confirmation channel during that invocation.

## Completion summary

A concise Markdown handoff with bullets under Completed, Decisions, Open items, and Next activity.

It states:

- Each role slice and what it contains.
- The steps and the assertions underneath them.
- Questions closed as non-critical, and why.
